# Route: oddness 6

*Working notes, started 2026-09-12. Companion to the survey (§3, §9 route 1). Code: `tools/oddness.py`.*

## 0. Target

> **(O6)** Every cyclically 6-edge-connected cubic graph with oddness at most 6 has a nowhere-zero 5-flow.

Known: oddness $\le 2$ (Jaeger 1988), oddness $\le 4$ with cyclic 6-edge-connectivity (Mazzuoccolo–Steffen 2017, "MS"), and cyclic connectivity $\ge \tfrac52\omega-3$ (Steffen 2010). Since a smallest counterexample to the 5-flow conjecture is cyclically 6-edge-connected (Kochol), (O6) would push its oddness to $\ge 8$. It is a natural next step but not obviously within reach of the MS method, as explained in §2. **Status (updated 2026-09-13; see §12):** the fixed-2-factor template is refuted by an explicit cyclically 6-edge-connected instance on 60 vertices, so any proof of (O6) by this method must use the 0-edge or 2-factor freedom. Earlier status:  the method's obstructions are fully catalogued (§2, §6); the four-class cover problem is solved for covers by 6-cuts (Lemma 6.5, conditional on the MS uncrossing claims); the 7- and 11-cut cases are open; no counterexample to the fixed-2-factor template was found in about 60,000 instances (§4).

## 1. The method (Steffen 2010, MS 2017), restated

**Balanced valuations (Bondy, Jaeger).** $w\colon V\to\mathbb R$ is balanced if $|\sum_{v\in X}w(v)|\le|\partial X|$ for all $X$. Jaeger: $G$ has a nowhere-zero $k$-flow iff it has a balanced valuation of the form $w(v)=\tfrac{k}{k-2}(2d^+(v)-d(v))$ for some orientation. For cubic $G$ and $k=5$: **$G$ has a nowhere-zero 5-flow iff some 2-colouring $V=A\cup B$ (white/black) makes $w=\mp\tfrac53$ balanced**, i.e. $5\,|b_X-a_X|\le 3\,|\partial X|$ for all $X$, where $a_X=|X\cap A|$, $b_X=|X\cap B|$. Note this forces $|A|=|B|$ and, for $|\partial X|\le 5$, $|b_X-a_X|\le 3$.

**Flow partitions.** Fix a 2-factor $F_2$ with odd circuits $C_1,\dots,C_{2t}$ and even circuits, and the complementary perfect matching $M$. A *canonical colouring* $c$ gives colour 1 to $M$, colours 2,3 alternately along each circuit, except that on each odd circuit one chosen edge (the *0-edge*) gets colour 0. Exactly one vertex per odd circuit misses colour 2; call them $z_1,\dots,z_{2t}$. The subgraph $H=c^{-1}(1)\cup c^{-1}(2)$ has all degrees $\le2$, degree 1 exactly at the $z_i$; so $H$ is a disjoint union of even circuits and $t$ paths $P_1,\dots,P_t$ of odd length pairing up the $z_i$ (the pairing is dictated by $G$, $F_2$, $c$).

MS build a nowhere-zero 4-flow on $G$ plus two parallel edges per path and read off a balanced $\pm2$ valuation. Unwinding their construction (I checked this from the definitions): the resulting black/white partition is exactly a **proper 2-colouring of $H$**, each component of $H$ coloured independently (their Lemma 2.4 is the statement that recolouring one path is allowed; recolouring an even circuit is equally allowed since the circuit orientations in the 4-flow are arbitrary). The vertex classes of the 2-colouring of $H$ are automatically equal in size on each component (even circuits, and odd-length paths have an even number of vertices), so $|A|=|B|$.

So the **method** is: choose $F_2$, choose the 0-edges, choose a 2-colouring of each component of $H$, set $w=\pm\tfrac53$, and hope $w$ is balanced. The whole freedom is
$$\underbrace{\text{2-factors with }2t\text{ odd circuits}}_{\text{huge}}\times\underbrace{\prod_i|C_i|}_{\text{0-edges}}\times\underbrace{2^{\#\text{components of }H}}_{\text{colourings}} .$$
MS use only the $2^t$ path recolourings for a fixed $F_2$ and $c$ (and only $t=2$).

**What can go wrong.** Suppose $w$ is not balanced, witnessed by $S$ with $5k>3m$, $k=|b_S-a_S|$, $m=|\partial S|$. Let $c_i=|\partial S\cap c^{-1}(i)|$. Since colour-1 and colour-2 edges are bichromatic, $k\le c_1$ and $k\le c_2+q$, where $q$ is the colour imbalance among the $z$'s inside $S$; also $k\equiv m\pmod 2$. Adding, $2k\le m+q$, hence $q>m/5$, and since $q\le t$ we get **$m<5t$**: bad cuts have fewer than $5t$ edges. Steffen's Proposition 1 refines this by residues mod 5, and with cyclic 6-edge-connectivity MS show for $t=2$ that a bad cut has exactly 6 edges, 4 of colour 1 and 2 of colour 2, and separates the $z$'s into two same-coloured pairs.

## 2. What changes at oddness 6 ($t=3$)

- **Bad cut sizes.** $m<15$; Steffen's Proposition 1 with $\omega=6$ gives $m\le11$ if $m\equiv1$, $m\le7$ if $m\equiv2$, $m\le8$ if $m\equiv3$ (mod 5). One can do better by direct case analysis. Use: $k>\tfrac35m$, $k\le c_1$, $k\le c_2+q$, $c_1+c_2\le m$, $q\le3$, and the two parity facts $k\equiv m\equiv c_1\pmod 2$ (colour 1 is a perfect matching, so $c_1\equiv|S|\equiv m$). Running through $m=6,\dots,14$ leaves exactly five possibilities:

  | $m$ | $k$ | $c_1$ | $c_2$ | $q$ | $c_0+c_3$ | tight? |
  |---|---|---|---|---|---|---|
  | 6 | 4 | 4 | 2 | 2 | 0 | $k=c_1=c_2+q$ |
  | 6 | 4 | 4 | 2 | 3 | 0 | $k=c_1$ |
  | 6 | 4 | 4 | 1 | 3 | 1 | $k=c_1=c_2+q$ |
  | 7 | 5 | 5 | 2 | 3 | 0 | $k=c_1=c_2+q$ |
  | 11 | 7 | 7 | 4 | 3 | 0 | $k=c_1=c_2+q$ |

  **Catalogue (t = 3, cyclically 6-edge-connected):** a bad cut has $(m,c_1,c_2,q)\in\{(6,4,2,2),(6,4,2,3),(6,4,1,3),(7,5,2,3),(11,7,4,3)\}$; in particular *there is no bad 8-cut*, and $k=c_1$ always, which means every colour-1 edge of the cut has its $S$-end black; where $k=c_2+q$ is tight, also every colour-2 cut edge has its $S$-end black and every $z$ in $S$ is black. The experiments of §4 found every one of these five types and nothing else.
- **The colour imbalance $q$** of $S\cap Z$: paths of $H$ with both ends in $S$ contribute 0; a path $P_i$ with exactly one end in $S$ contributes $\varepsilon_i\sigma_i=\pm1$ where $\varepsilon_i$ is the chosen colouring of $P_i$ and $\sigma_i$ records which end is in $S$. So $q\ge2$ needs at least two paths separated by $\partial S$ with equal signs; $q=3$ needs all three separated with equal signs.
- **Constraint system.** Write $J(S)\subseteq\{1,2,3\}$ for the paths separated by $\partial S$. A cut with $J=\{a,b\}$ is bad for the colourings with $\varepsilon_a\varepsilon_b=\sigma_a\sigma_b=:s(S)$; a cut with $J=\{1,2,3\}$ is bad for the colourings with $\varepsilon_1\sigma_1=\varepsilon_2\sigma_2=\varepsilon_3\sigma_3$. Up to global complement there are 4 colourings $\varepsilon$ of the three paths. The method (with $F_2,c$ fixed and only path recolourings) succeeds iff the system of "forbidden" relations has a solution. It has **no** solution in exactly these situations:
  1. two bad cuts with the same pair $J=\{a,b\}$ and opposite signs $s$ (this is the only obstruction at $t=2$, and MS's whole proof is that it cannot happen);
  2. three bad cuts with $J=\{1,2\},\{1,3\},\{2,3\}$ and $s_{12}s_{13}s_{23}=+1$ (each forbids one parity; the three forced parities are inconsistent);
  3. combinations with $J=\{1,2,3\}$ cuts (each forbids two of the eight $\varepsilon$, i.e. one class up to complement) together with pair cuts.
- **Why the MS argument does not transfer.** Their final contradiction takes two bad cuts $E_3,E_4$ for $J=\{1,2\}$ with opposite signs, uncrosses them into $U_1,\dots,U_4$ using cyclic 6-edge-connectivity to force $|\partial(U_i,U_j)|\in\{0,3\}$, and then observes that $E_3'\cup E_4'$ is a 6-cut inside $H$ crossed an odd number of times by $P_1\cup P_2$ while every circuit of $H$ crosses it evenly. With a third path $P_3$ in $H$, $P_3$ may cross $E_3'\cup E_4'$ an odd number of times, and the parity contradiction disappears. So even obstruction 1 needs a new argument at $t=3$, and obstructions 2 and 3 are new.
- **Extra freedom not used by MS.** The 0-edge on each odd circuit (which moves the $z_i$ around the circuit and changes the paths), the colourings of the even circuits of $H$, and the choice of $F_2$ itself. Any of these may be needed.

## 3. Decisive experiment before theory

Before trying to prove (O6) with the flow-partition method it should be checked that the method is *capable* of it: for cyclically 6-edge-connected cubic graphs with a 2-factor of exactly 6 odd circuits, is there always a balanced flow partition, and if so, is it always reachable by path recolourings alone (as in MS), or does one need the 0-edge/even-circuit/2-factor freedom? `tools/oddness.py` implements the method exactly (canonical colouring, $H$, 2-colourings, the balance test as one min-cut) and, as a check, reproduces Jaeger's oddness-2 theorem: on Petersen, $J_7$ and $J_9$ every sampled 2-factor with 2 odd circuits and every 0-edge choice gives a balanced partition (150 + 22,354 + 71,657 cases).

Test graphs: generalized Petersen graphs $GP(n,3)$ and $GP(n,4)$ with $n$ odd (girth 6 or 7, so cyclic connectivity is plausibly 6 or more; they are 3-edge-colourable but have 2-factors with 4 or 6 odd circuits, which is all the fixed-$F_2$ template sees), the flower snarks $J_7,J_9$ (cyclically 6-edge-connected, oddness 2), and the oddness-6 snark $R_2$ on 40 vertices of Lukoťka–Máčajová–Mazák–Škoviera (cyclic connectivity only 2, so a failure there would not contradict (O6), but a success is informative). Results are recorded in §4 as they come in.

## 4. Experiment log

**Test-bed problem.** Vertex-transitive families are useless here: with girth 6 every odd circuit has $\ge7$ vertices, and $J_{11}$ has a single odd circuit of length $\le11$, $GP(29,3)$ none. So I generate graphs with a *planted* 2-factor: prescribed circuit lengths plus a random perfect matching chosen edge by edge among vertices at distance $\ge5$ (girth $\ge6$), then certify cyclic 6-edge-connectivity exactly by SAT (no vertex set $X$ with $6\le|X|\le n-6$ and $d(X)\le5$; with girth $\ge6$ this is equivalent). Two flavours (`tools/gen_cyc6.py`): fully random matchings, and **rings of blocks** joined by exactly 3 matching edges between consecutive blocks, which have the crossing 6-cut structure ($U_1,\dots,U_4$ with $|\partial(U_i,U_{i+1})|=3$) that drives the MS obstruction. Blocks must contain an even number of odd circuits, so rings of 3 blocks with 2 odd circuits each give the oddness-6 test cases.

| Graphs | 2-factor | Result |
|---|---|---|
| Petersen, $J_7$, $J_9$ | all sampled 2-factors with 2 odd circuits, all 0-edges (150 + 22,354 + 71,657 cases) | balanced flow partition always exists (Jaeger's theorem reproduced) |
| $R_2$ (40 vertices, oddness 6, cyclic connectivity 2) | 165 planted 2-factors with 6 odd circuits, 400 0-edge choices each | every 2-factor has some balanced flow partition; **61,999 of 66,000 (2-factor, 0-edge) pairs** have one (the rest are failures or misses of the 64-colouring sample), so the 0-edge choice matters on graphs with small cuts |
| 6 random cyclically 6-connected graphs, planted $6\times C_7+C_8$, $n=50$ | 120 0-edge choices each | path recolourings alone always balanced (120/120 on every graph) |

| 29 random cyclically 6-connected graphs, planted profiles $6\times C_7+2C_6$, $4C_7+2C_9+C_6$, $6\times C_9$ ($n=52$–$54$) and $4C_7+C_8+C_6$, $4C_9+2C_6$ (4 odd circuits, $n=42$–$48$) | 120 0-edge choices each | path recolourings alone always balanced |
| 20 rings of 3 blocks $[7,7,6]$, $[7,9,8]$, $[7,7,8]$, $[7,7,6,6]$ ($n=60$–$78$), all block boundaries = 3 matching edges | 120 0-edge choices each | path recolourings alone always balanced. Expected in hindsight: a cut made of matching edges has $c_2=0$, so it is never bad |
| 8 four-block rings with a $C_7$ straddling two consecutive boundaries (profile A, $n=54$), which contain two *crossing bad-type* 6-cuts (4 matching + 2 circuit edges), exactly the $U_1..U_4$ configuration of MS | 120 0-edge choices each | path recolourings alone always balanced |

| 14 more four-block straddle rings (profiles C, D: $n=56$–$60$) | 120 0-edge choices each | path recolourings alone always balanced |

So far the fixed-$F_2$, path-recolouring template has not failed on any cyclically 6-connected graph with a 6-odd 2-factor, including ones built to contain the MS crossing configuration.

**What the violating cuts look like** (`tools/badcuts.py`: for each of the 8 path colourings, extract the minimum-cut witness $S$ and classify it by $(|\partial S|, c_1, c_2, |S\cap Z|, q, \text{paths separated})$; 360 instances per profile, six graphs each):

| type $(|\partial S|,c_1,c_2,|S\cap Z|,q,J)$ | straddle A | straddle C | straddle D | straddle F | random $6C_7{+}C_8$ |
|---|---|---|---|---|---|
| $(11,7,4,3,3,\{1,2,3\})$ | 161 | 191 | 173 | 153 | 67 |
| $(7,5,2,3,3,\{1,2,3\})$ | 16 | 12 | 17 | 3 | 1 |
| $(6,4,2,2,2,\{a,b\})$ | 26 | 22 | 23 | 3 | 0 |
| $(6,4,1,3,3,\{1,2,3\})$ | 2 | 0 | 0 | 0 | 0 |
| **min. number of balanced colourings among the 8** | 4 | 4 | 4 | 4 | 6 |

Looking at the 11-cut witnesses concretely (straddle A): $S$ consists of one whole odd circuit together with an arc of each of two other odd circuits, each arc containing that circuit's $z$, and *all seven* matching edges at these 15 vertices leave $S$; the four circuit edges of the cut are the ends of the two arcs, coloured 2 on both sides by the 0-edge placement. So the obstruction is local: three "$z$-neighbourhoods" of the same colour glued by the matching. Moving a 0-edge along one of the two partial circuits changes both which vertex is $z$ and the colour parity of the arc ends, so the 0-edge freedom acts directly on this cut type.

Three things stand out.

1. **The dominant obstruction at oddness 6 is new**: an 11-edge cut with 7 matching edges and 4 colour-2 edges whose side contains exactly one end of each of the three paths, all of the same colour ($k=7=c_1=c_2+q$, tight everywhere). It is impossible at oddness 4 ($q\le2$) and it appears even in random cyclically 6-connected graphs. It forbids exactly the two colourings in which the three ends in $S$ agree, i.e. one of the four classes up to complement.
2. The MS-type bad 6-cuts $(6,4,2,2,2,\{a,b\})$ do occur, in the straddle rings, for each of the three pairs, and each forbids two of the four classes. The 7-cut and $(6,4,1)$ types are rarer.
3. **Co-occurrence** (`tools/killsets.py`, which records for each instance the set of killed classes and the cut types responsible): in the four-block straddle rings, 1 class is killed in ~30% of instances, 2 classes in ~5%, and in one instance out of 1,080 **3 classes** were killed (only 2 of the 8 colourings balanced). Two 11-cuts with *different* side patterns do coexist in one instance, so there is no "all 11-cuts have the same pattern" lemma to hope for; the combinations seen are 11+11, 11+7, 11+6(pair), 6(pair)+6(pair) and 6+7. On the 6-block rings (`six12`, `six10`, $n=96$–$102$), which contain three crossing bad-type 6-cuts, instances with only 2 balanced colourings appear at a rate of about 1 in 250, and a new type $(6,4,2,4,2,\{a,b\})$ (a 6-cut with four $z$'s inside) shows up.

   In every instance so far at least 1 of the 4 classes survives.

4. **Full-method run on the 6-block rings** (`six12`, `six10`; 32 graphs, 250 0-edge choices each = 8,000 instances): path recolourings alone always produced a balanced partition. A second seed (`SEED=23`) over `six12`, `six10`, `straddleA`, `straddleC` (64 graphs, 16,000 instances) gave the same: no instance in which all 8 path colourings fail.

**Summary of the evidence for the plan in §5:** outcome (a) is what the data supports so far: on every cyclically 6-connected test graph, for every planted 6-odd 2-factor and 0-edge choice tried (about 35,000 instances), some recolouring of the three paths gives a balanced valuation, even though up to three of the four colouring classes can be killed. So the fixed-$F_2$ template is not refuted at oddness 6, and the theoretical task is to prove that the five bad-cut types cannot jointly kill all four classes. Killing all four would need, e.g., a pair-type 6-cut plus two 11-cuts with different side patterns, or four 11-cuts with the four distinct patterns. Whether cyclic 6-edge-connectivity forbids such combinations is exactly what a proof of (O6) has to show; the 6-block rings (three crossing bad-type 6-cuts) are the test for it.


## 5. Plan

1. Run the experiment of §3. Three possible outcomes: (a) path recolourings always suffice at $t=3$ on cyclically 6-connected graphs: then (O6) is a case analysis of obstructions 1–3 above, and the job is to find the replacement for the parity argument; (b) they do not suffice but 0-edge/even-circuit freedom does: then the proof must first choose $c$ well, and the experiment tells which choices work; (c) some cyclically 6-connected graph with a 6-odd 2-factor has no balanced flow partition at all: then (O6), if true, needs a different valuation family, and the template is dead at oddness 6.
2. If (a): prove obstruction 2 impossible via uncrossing three cuts of size 6–11; prove obstruction 1 with $P_3$ present by controlling the parity of $|E(P_3)\cap(E_3'\cup E_4')|$, possibly by choosing $\varepsilon_3$ or the 0-edge of $C_5,C_6$.
3. Either way, record the smallest configurations found; they are the "bad 6-cut" analogues that a proof must enumerate.

## 6. Towards a proof that the four classes cannot all be killed

Fix $G$ cyclically 6-edge-connected cubic, $F_2$ with 6 odd circuits, the canonical colouring $c$, $H$, the three paths $P_i$ from $z_{2i-1}$ to $z_{2i}$, and a colouring of the even circuits of $H$. For $\varepsilon\in\{\pm\}^3$ let $w_\varepsilon$ be the valuation. Write "$S$ is tight for $\varepsilon$" if $S$ is a bad cut for $w_\varepsilon$, i.e. $5(b_S-a_S)>3|\partial S|$ with $b_S>a_S$.

**Lemma 6.1 (tightness).** If $S$ is tight for $\varepsilon$ then $(m,c_1,c_2,q)$ is one of $(6,4,2,2)$, $(6,4,1,3)$, $(7,5,2,3)$, $(11,7,4,3)$; in all cases $k=b_S-a_S=c_1$, so the $S$-end of every colour-1 edge of $\partial S$ is black, and except for the first type also $k=c_2+q$, so the $S$-end of every colour-2 edge of $\partial S$ is black and every $z\in S$ on a separated path is black. In the first type the same holds because $k=4=c_2+q$ there too. Moreover $\partial S\subseteq E(H)$ except in type $(6,4,1,3)$, where exactly one edge of $\partial S$ has colour 0 or 3.

*Proof.* The table in §2 gives the five candidates. The candidate $(6,4,2,3)$ has $\partial S\subseteq E(H)$ with $|\partial S|=6$ even; every even circuit of $H$ crosses $\partial S$ an even number of times and a path crosses it an odd number of times iff it is separated; $q=3$ means three separated paths, an odd total, contradiction. The tightness statements are the equality cases of $k\le c_1$ and $k\le c_2+q$ (vertices of $S$ not matched inside $S$ by colour 1, resp. not covered by a colour-2 edge inside $S$). $\square$

**Lemma 6.2 (alternation).** Let $S$ be tight for $\varepsilon$ and let $K$ be a component of $H$ (a path or an even circuit) that crosses $\partial S$. Then along $K$ the crossing edges alternate in colour $1,2,1,2,\dots$, every segment of $K$ inside $S$ has an odd number of vertices with both end-vertices black, and every segment outside $S$ (between two crossings) has an odd number of vertices with both end-vertices white. For a path $P_i$: if $P_i$ is separated by $S$ it crosses an odd number of times and its first and last crossing edges have colour 1; if it is not separated it crosses an even number of times (possibly zero) and, when it crosses, the crossing edge nearest one end has colour 1 and the one nearest the other end has colour 2.

*Proof.* The end-vertices of an inside segment are $S$-ends of crossing edges, hence black by Lemma 6.1; a path in $H$ between two vertices of the same colour has even length, so the segment has an odd number of vertices and the two bounding crossing edges are an odd number of positions apart along $K$; colours alternate along $K$, so they differ. Outside segments are the same with white. For a path end $z$: the end segment from $z$ to the nearest crossing has odd length iff $z$ has the colour appropriate to its side (black inside, white outside); the nearest crossing edge then has colour 1 (the colour of the edge at $z$), otherwise colour 2. A separated path has $z\in S$ black and $z'\notin S$ white (Lemma 6.1), so both nearest crossings have colour 1 and the number of crossings is odd. A non-separated path has its two ends of opposite colour on the same side, so exactly one end is "appropriate", giving colours 1 and 2 at the two extremes and an even number of crossings. $\square$

Both lemmas were checked mechanically on every bad cut of 280 generated instances (`tools/lemmas_check.py`): no violation.

**Corollary 6.3 (what a tight cut kills).** Let $J^*(S)$ be the set of paths that cross $\partial S$. $S$ is tight for exactly the colourings $\varepsilon$ that agree with one fixed pattern $\sigma_S$ on $J^*(S)$: flipping any path of $J^*(S)$ turns an inside segment black-ended into white-ended and lowers $k$ by at least 2, below the threshold ($k=4,5,7$ are the minimum admissible values for $m=6,7,11$). Flipping a path disjoint from $\partial S$ changes nothing. Hence a tight cut kills $2^{3-|J^*(S)|}$ colourings, i.e. (up to complement) one class if $|J^*(S)|=3$ and two classes if $|J^*(S)|=2$. Since $q\ge2$, $|J^*(S)|\ge2$. The types with $q=3$ have $|J^*|=3$; a pair-type cut $(6,4,2,2)$ has $|J^*|=2$ iff the third path avoids $\partial S$ entirely. In the generated instances most pair-type cuts are crossed by the third path (24 of 27 on the 6-rings), so they kill only one class.

**The cover problem.** Writing a class as $(x,y)=(\varepsilon_1\varepsilon_2,\varepsilon_1\varepsilon_3)$, a codimension-2 cut on the pair $\{1,2\}$ with sign $s$ kills $\{x=s\}$, on $\{1,3\}$ kills $\{y=s\}$, on $\{2,3\}$ kills $\{xy=s\}$; a codimension-3 cut kills one point. All four classes are killed only in one of these situations:

1. two codim-2 cuts on the same pair with opposite signs;
2. three codim-2 cuts on the three pairs with $s_{12}s_{13}s_{23}=+1$;
3. two codim-2 cuts on different pairs (3 classes) plus a codim-3 cut;
4. one codim-2 cut plus two codim-3 cuts;
5. four codim-3 cuts with the four distinct patterns.

**Lemma 6.4 (situation 1 is impossible).** This is the Mazzuoccolo–Steffen argument. Two codim-2 cuts $E_3=\partial S$, $E_4=\partial S'$ on the pair $\{1,2\}$ with opposite signs give, after complementing, $S\cap Z\supseteq\{z_1,z_3\}$, $S'\cap Z\supseteq\{z_1,z_4\}$ and $z_2\notin S\cup S'$. Their Claims 3.4 and 3.6–3.8 use only that both are 6-cuts with four colour-1 and two colour-2 edges and cyclic 6-edge-connectivity, and they give the four parts $U_1\ni z_1$, $U_2\ni z_2$, $U_3\ni z_3$, $U_4\ni z_4$ with $|\partial(U_1,U_2)|=|\partial(U_3,U_4)|=0$ and the other four part-cuts of size 3. The final step uses that $\partial U_1\subseteq E_3\cup E_4\subseteq E(H)$ is a 6-cut crossed an odd number of times by $P_1\cup P_2$ and an even number of times by each even circuit of $H$. Here the third path $P_3$ is disjoint from $E_3\cup E_4$ by the codimension-2 assumption, so it crosses $\partial U_1$ zero times, and the parity contradiction stands. $\square$

So situation 1 cannot occur; in the data it never did. The remaining situations 2–5 all involve at least one cut of size 7 or 11 or a codim-3 pair cut, and this is where the argument has to be extended. What is known from the experiments: situations 3 and 4 have been seen *partially* (three classes killed, e.g. an 11-cut together with a codim-2 pair cut, or two 11-cuts and a 7-cut) but never completed. Two 11-cuts with different patterns do coexist, so situation 5 cannot be excluded by a "two 11-cuts uncross to a contradiction" lemma; it needs a statement about three or four.

**Candidate lemmas (open).**

- (L-A) Two codim-3 cuts $S,S'$ tight for classes that differ on exactly one path $P_p$ satisfy: $P_p$ crosses $\partial(S\cap S')$ and $\partial(S\cup S')$ ... [to be found]. The natural quantities are the sizes of the four part-cuts of $S,S'$; submodularity gives $d(S\cap S')+d(S\cup S')\le 22$ for two 11-cuts, which is not restrictive on its own; the colour counts of Lemma 6.2 on the uncrossed parts are the extra information.
- (L-B) In situation 2, the three pair cuts $S_{12}\ni z_1,z_3$, $S_{13}\ni z_1,z_5$, $S_{23}\ni z_3,z_5$ with each third path avoiding its cut: $P_3\subseteq S_{12}$ or $P_3\cap S_{12}=\emptyset$, and similarly for the other two. Uncrossing $S_{12}$ with $S_{13}$ as in Lemma 6.4 gives parts containing $z_1$; $z_2$; $z_3$; $z_5$ with the same 3/0 structure, and $z_4,z_6$ located by the avoidance conditions; then $S_{23}$ has to be placed against this structure. This looks like a finite case analysis of the MS kind and is the first thing to try.
- (L-C) **Crossing profiles.** Let $r_i$ be the number of times $P_i$ crosses $\partial S$ and $s$ the number of colour-1 (equivalently colour-2) crossings by even circuits of $H$. By Lemma 6.2 a separated path contributes $(r_i+1)/2$ colour-1 and $(r_i-1)/2$ colour-2 edges, a non-separated one $r_i/2$ of each. Matching the counts $(c_1,c_2)$ gives, for the pair type $(6,4,2)$ with separated paths $a,b$: $r_a+r_b+r_3+2s=6$, so $(r_a,r_b,r_3,s)\in\{(1,1,0,2),(1,1,2,1),(1,1,4,0),(3,1,0,1),(3,1,2,0),(3,3,0,0),(5,1,0,0)\}$; the codimension-3 pair cuts are those with $r_3\ge2$. For the 7-cut $(5,2)$: $r_1+r_2+r_3+2s=7$ with all $r_i$ odd, so $(r_1,r_2,r_3,s)\in\{(1,1,1,2),(3,1,1,1),(3,3,1,0),(5,1,1,0)\}$ up to order. For the 11-cut $(7,4)$: $r_1+r_2+r_3+2s=11$, all $r_i$ odd. For $(6,4,1,3)$ the non-$H$ edge is a colour-0 or colour-3 edge of an $F_2$ circuit, and $r_1+r_2+r_3+2s=5$, all $r_i$ odd: $(1,1,1,1)$, $(3,1,1,0)$.

These crossing profiles are what an uncrossing argument would work with. I have not carried it out.

**Where cyclic 6-edge-connectivity enters.** In the above, cyclic 6-connectivity was used only to (a) exclude bad cuts of size $\le5$ and (b) obtain the part-cut sizes in Lemma 6.4. Actually (a) needs no connectivity assumption at all: for $m=5$ one needs $k\ge5=c_1=m$, hence $c_2=0$ and $k\le q\le3$, impossible; for $m=4$, $k=4=c_1=m$, $c_2=0$, $k\le q\le 3$, impossible. Cuts of size 2 and 3 can be bad ($k=2=c_1$, $q\ge2$; $k=3=c_1$, $q=3$), which is what happens on $R_2$. **So the catalogue of Lemma 6.1 holds for every cyclically 4-edge-connected cubic graph**, and cyclic 6-edge-connectivity is used only in interaction lemmas such as Lemma 6.4.

Experiment on cyclically 5-connected (girth 5) graphs of the profiles `straddleA`, `straddleC`, `six12` (`CYC5=1 tools/killsets.py`, 1,440 instances): the same four cut types, no others; killed-class counts $\{0{:}757,\ 1{:}506,\ 2{:}162,\ 3{:}15\}$; **no instance with all four classes killed**, but 3-kills are ten times more frequent than on the cyclically 6-connected graphs. So either the four-class statement is true already at cyclic 5-connectivity, or the counterexamples are rarer than 1 in 1,440 there.

**Local lemma for two codimension-3 cuts.** Let $S,S'$ be tight for $\varepsilon,\varepsilon'$ where $\varepsilon'$ is $\varepsilon$ with $P_3$ flipped (after complementing, this is the general case of two classes differing in one coordinate), and put $A=S\cap S'\ni z_1,z_3$, $B=V\setminus(S\cup S')\ni z_2,z_4$, $C=S\setminus S'\ni z_5$, $D=S'\setminus S\ni z_6$. Since $\partial S,\partial S'\subseteq E(H)$ (for the 11- and 7-types) every edge between parts is an $H$-edge, bichromatic under both colourings. Then: an $A$–$B$ edge lies in both cuts, so its $A$-end is black under $\varepsilon$ and under $\varepsilon'$, hence **not on $P_3$**; a $C$–$D$ edge lies in both cuts, its $C$-end is black under $\varepsilon$ and its $D$-end black under $\varepsilon'$, which is only consistent if **both ends lie on $P_3$**; for $P_3$ itself, indexing its vertices $v_0=z_5,\dots,v_L=z_6$ (black under $\varepsilon$ iff the index is even), an edge of $P_3$ of type $C$–$B$ or $A$–$D$ has its $C$- or $A$-end at an even index, of type $A$–$C$ or $B$–$D$ its $A$- or $D$-end at an odd index, and $P_3$ has no $A$–$B$ edge; $P_1,P_2$ have no $C$–$D$ edge. The parities $|\partial A|,|\partial B|$ even and $|\partial C|,|\partial D|$ odd follow from which paths each part separates. I checked that these conditions are *consistent* (e.g. $|AB|=4,|AD|=3,|CB|=2,|CD|=2,|AC|=1,|DB|=4$), in line with the observed coexistence of two 11-cuts; a contradiction can only come from three or four cuts at once, or from an inequality on part-cuts using cyclic 6-connectivity.

## 7. Stress test at higher oddness: does the four-class phenomenon scale?

The proof strategy of §6 is "the tight cuts cannot jointly kill all $2^{t-1}$ classes". If that were a general truth for every $t$ on cyclically 6-edge-connected graphs, the path-recolouring template would prove the 5-flow conjecture for all cyclically 6-edge-connected cubic graphs, which nobody expects to be this easy. So it is worth knowing *how* it fails, or fails to fail, at oddness $8,10,12,14$: the first oddness at which the killed classes reach $2^{t-1}$ tells where the real difficulty sits, and the cut combinations that get closest are the model for what a proof at $t=3$ must exclude.

**Catalogue at oddness $2t$.** The inequalities of §2 ($5k>3m$, $k\le c_1$, $k\le c_2+q$, $c_1+c_2\le m$, $q\le t$, $k\equiv m\equiv c_1 \bmod 2$) give the following admissible *all-tight* types $(m,c_1,c_2,q)$ with $k=c_1=c_2+q$ (the non-tight combinations are the same $m$ with larger $c_1$ or $c_2$ and are ruled out or not by the same parity arguments as in Lemma 6.1; every type seen in the experiments is all-tight). Bad cut sizes are $m<5t$; for each $k$ the sizes are $2k-t\le m<5k/3$ with $m\equiv k \bmod 2$.

| $t$ | new all-tight types at this $t$ (beyond those at $t-1$) |
|---|---|
| 2 | $(6,4,2,2)$ |
| 3 | $(6,4,1,3)$, $(7,5,2,3)$, $(11,7,4,3)$ |
| 4 | $(6,4,0,4)$, $(7,5,1,4)$, $(8,6,2,4)$, $(11,7,3,4)$, $(12,8,4,4)$, $(16,10,6,4)$ |
| 5 | $(7,5,0,5)$, $(8,6,1,5)$, $(9,7,2,5)$, $(11,7,2,5)$, $(12,8,3,5)$, $(13,9,4,5)$, $(16,10,5,5)$, $(17,11,6,5)$, $(21,13,8,5)$ |
| 6 | $(8,6,0,6)$, $(9,7,1,6)$, $(10,8,2,6)$, $(11,7,1,6)$, $(12,8,2,6)$, $(13,9,3,6)$, $(14,10,4,6)$, $(16,10,4,6)$, $(17,11,5,6)$, $(18,12,6,6)$, $(21,13,7,6)$, $(22,14,8,6)$, $(26,16,10,6)$ |

Two patterns. (i) The **largest** bad cut at oddness $2t$ has $m=5t-4$, $k=3t-2$, $c_2=2t-2$, $q=t$: $(6,4,2,2)$, $(11,7,4,3)$, $(16,10,6,4)$, $(21,13,8,5)$, $(26,16,10,6)$. Concretely it is the union of $t$ same-coloured "$z$-arcs" glued by the matching, exactly as the 11-cut of §4 is three such arcs; it separates all $t$ paths and kills one class. (ii) The **smallest** ones, $m\in\{6,7,8,\dots\}$ with $c_2\in\{0,1,2\}$, are cuts consisting almost entirely of matching edges and separating $q\ge m-2$ paths; at $t=4$ the new pure-matching type $(6,4,0,4)$ appears: six matching edges, $S$ containing one end of each of four paths, all four black. It is the first bad cut that is a *cyclic 6-cut made of matching edges only*, which is why the rings of blocks joined by 3+3 matching edges, harmless at $t=3$ ("a cut of matching edges has $c_2=0$ so is never bad", §4), become dangerous at $t\ge4$.

Each all-tight cut separating $q$ paths fixes the relative colours of those $q$ paths, so it kills a fraction $2^{1-q}$ of the $2^{t-1}$ classes (Corollary 6.3 with $|J^*|=q$; a cut crossed by more paths than it separates kills less). Killing all classes needs the killed sets to cover: at $t=4$ two $q=2$ cuts with complementary constraints suffice (the MS situation 1, excluded by Lemma 6.4 when both are pair cuts with the other paths avoiding them, but at $t\ge4$ a $q=2$ cut is typically crossed by further paths and then kills only a quarter or an eighth), or four $q=3$ cuts, or eight $q=4$ cuts with the eight distinct patterns.

**Experiment log at higher oddness** (`tools/killsets_t.py`, `tools/run_oddness.py` with the `t4…t8` profiles; cyclically 6-edge-connected by SAT as before; 40 to 100 0-edge choices per graph, all $2^{t-1}$ path-colouring classes tested per instance; "worst" = largest number of killed classes in one instance).

| oddness $2t$ | classes | family | graphs × 0-edges | instances with 0 killed | worst kill |
|---|---|---|---|---|---|
| 8 | 8 | random $8C_7{+}C_8{+}C_6$ ($n=70$) | 16 × 40 | 555 / 640 | 3 |
| 8 | 8 | 5-block straddle rings ($n=68$) | 16 × 40 | 194 / 640 | 6 |
| 8 | 8 | 4-block rings $[7,7,6]$ ($n=80$) | 16 × 40 | 130 / 640 | **7** |
| 10 | 16 | 6-block straddle rings | 12 × 40 | 33 / 480 | 13 |
| 10 | 16 | 5-block rings $[7,7,6]$ ($n=100$) | 4 × 40 | 7 / 160 | **15** |
| 12 | 32 | random $12C_7{+}C_8{+}C_6$ ($n=98$) | 4 × 40 | 73 / 160 | 6 |
| 12 | 32 | 6-block rings $[7,7,6]$ ($n=120$) | 4 × 40 + 8 × 100 | 17 / 960 | **30** |
| 12 | 32 | 6-block rings $[7,7,8]$ | 4 × 40 | 3 / 160 | 30 |
| 12 | 32 | 3-block rings $[7,7,7,7,6]$ ($n=102$) | 4 × 60 | 12 / 240 | 24 |
| 14 | 64 | random $14C_7{+}C_8$ ($n=106$) | 4 × 40 | 19 / 160 | 29 |
| 14 | 64 | 7-block rings $[7,7,6]$ ($n=140$) | 4 × 40 + 8 × 100 | 3 / 960 | **62** |
| 16 | 128 | 8-block rings $[7,7,6]$ ($n=160$) | 4 × 40 | 0 / 160 | **126** |
| 16 | 128 | 4-block rings $[7,7,7,7,6]$ ($n=136$) | 4 × 60 | 0 / 240 | 112 |

The full method (`run_oddness.py`, 100 to 150 0-edge choices per graph on the `t4rand`, `t4ring`, `t4str`, `t5rand`, `t6ring`, `t7str` profiles) never met an instance in which all path colourings fail, in agreement with the table: **in no instance were all classes killed**, at any oddness up to 16. (The `t5rand` run took 700 to 5,000 s per graph and the `t6`/`t7` runs did not start before the machine went down: the runner materialized the full product of 0-edge positions before sampling, which is $7^{10}\approx3\cdot10^8$ tuples for ten 7-circuits and $7^{12}$ for twelve; both runners now sample directly.)

Reading of the table:

1. **Random cyclically 6-connected graphs are harmless at every oddness tried**: at most a small fraction of the classes dies, mostly to the single maximal cut $(5t-4,3t-2,2t-2,t)$ of pattern (i) and to 11-cuts. The catalogue types seen are exactly the all-tight ones of the table above, and nothing outside it: at $t=7$ the types $(31,19,12,7)$, $(27,17,10,7)$, $(26,16,9,7)$, $(22,14,7,7)$ appear, at $t=8$ additionally $(20,14,6,8)$, $(19,13,5,8)$, $(19,13,6,7)$, $(6,6,0,6)$.
2. **Rings of blocks are the dangerous structure**, and they get relatively more dangerous as $t$ grows: the fraction of surviving classes in the worst instance is $1/8$, $1/16$, $2/32$, $2/64$, $2/128$. The killers there are the small cuts made mostly of matching edges, $(6,4,2,2)$, $(8,6,2,4)$, $(7,5,1,4)$, $(9,7,2,5)$, $(12,8,4,4)$, i.e. a block or a union of consecutive blocks with a few circuit edges added or removed, plus the 11-cuts inside blocks. With four odd circuits per block instead of two the rings are a little less deadly (24/32 and 112/128), presumably because the intervals of blocks then contain more path ends and their imbalance is harder to push to the threshold.
3. **Anatomy of the 7-of-8 instance at oddness 8** (`tools/survivors.py` on `worst_t4ring_23.json`, a 4-block ring $[7,7,6]$, $n=80$; paths $P_0$: blocks 0–3, $P_1$: 0–1, $P_2$: 3–2, $P_3$: 2–1). Exactly three cuts do all the killing:
   - a pair-type 6-cut $S_A$ = block 3 minus two vertices, separating $P_0,P_2$ and crossed by no other path, killing the four classes with $\varepsilon_2=1$;
   - an 11-cut $S_B$ (20 vertices of block 2, 7 of block 3) separating $P_0,P_2,P_3$ with $P_1$ disjoint from it, killing the two classes $(\varepsilon_1,\varepsilon_3)=(\ast,1)$ inside $\varepsilon_2=0$;
   - an 11-cut $S_C$ (20 vertices of block 0, 13 of block 3) separating $P_0,P_1,P_2$ with $P_3$ disjoint from it, killing the two classes $(\varepsilon_1,\varepsilon_3)=(1,\ast)$.

   The survivor is $(\varepsilon_1,\varepsilon_3)=(0,0)$. It survives for a *combinatorial* reason: inside the half $\varepsilon_2=0$ (a $2\times2$ grid indexed by $(\varepsilon_1,\varepsilon_3)$) the two 11-cuts each kill a line, one horizontal and one vertical, and two such lines always meet. A full kill by this kind of configuration needs the two 11-cuts to kill *parallel* lines, i.e. to separate the same triple of paths with patterns differing only on the third path, and to coexist with the pair cut. Are parallel pairs possible at all? I first wrote here that they had never been observed; that was wrong. Rerunning `tools/killsets.py` on the cyclically 6-connected profiles `six12`, `straddleA`, `straddleC` (1,080 instances, `kill_cyc6_sig.log`) with the per-class witness signatures gives:
   - killed-class counts $\{0{:}692,\ 1{:}332,\ 2{:}50,\ 3{:}6\}$;
   - **17 instances with two 11-cuts on the same triple whose patterns differ in exactly one path** (classes adjacent in the $2\times2$ grid, all four adjacencies occur), and 5 with two 11-cuts of diagonal patterns;
   - **3 instances in which three 11-cuts kill three of the four classes**, i.e. three of the four patterns of situation 5 realized at once, with only the fourth missing.

   So at oddness 6 the "local lemma" configuration of §6 exists, the three-pattern configuration exists, and the whole difficulty is concentrated in the last step: *given three 11-cuts realizing three patterns (or a codim-2 pair cut and one 11-cut in the other half), why can the remaining pattern not be realized by a tight cut?* The oddness-8 ring instance is the same phenomenon one level up.

4. The worst instances at oddness 12 and 16 are built the same way from 20 to 60 distinct witness cuts, nearly all inside one block or one pair of consecutive blocks, and the surviving classes always come in the same "two lines meet" pattern; I have not found a full kill in about 4,000 ring instances. In the 126-of-128 instance (`worst_t8ring_5.json`, 8-block ring, 44 distinct witness cuts) the two survivors differ only in the colouring of the one path that has both ends inside a single block; every cut that separates that path is a 12-, 9- or 11-cut reaching into the neighbouring blocks, and none of them realizes the missing pattern.

What this changes in the plan of §5: candidate lemma (L-A) in its strong form ("two codim-3 cuts cannot differ in one coordinate") is **false**, so no pairwise interaction lemma can finish situations 4 and 5; the statement to prove is about the *last* cut given the others. The three-11-cut instances (dumped by `killsets.py` as `threekill_*.json`) are the test cases: uncross the three cuts, compute the part sizes and the positions of the six $z$'s, and look for the invariant that blocks the fourth pattern. The same question at oddness 8 has the cleaner two-cut form of the ring instance above: a pair cut plus an 11-cut, and the missing parallel 11-cut.

**Correction (classes versus colourings).** The "four classes up to complement" bookkeeping of §6 and the tables of §7 merge each path colouring $\varepsilon$ with $-\varepsilon$. That is exact only when the complement is applied to *all* components of $H$. The tools keep the even circuits of $H$ at a fixed random colouring and flip the paths only, so $\varepsilon$ and $-\varepsilon$ are two different valuations. Let $s$ be the number of even-circuit segments of a tight cut $S$ inside $S$ (the $s$ of the crossing profiles in §6). For the path-flipped colouring the imbalance of $S$ becomes $2s-k$ and that of $V\setminus S$ becomes $k-2s$, which is below the threshold unless $s=0$. Hence a tight cut kills the $2^{t-|J^*(S)|}$ colourings with its pattern, and the complementary pattern only if no even circuit crosses it. The right statistic is therefore the number of killed colourings among all $2^t$ (with the even circuits fixed), and a failure of the path-recolouring template means all $2^t$ are killed. `run_oddness.py` always tested all $2^t$, so the headline "path recolourings never failed" is unaffected; `killsets_t.py` tested only the half with $\varepsilon_1=0$, so its "worst kill of $2^{t-1}$ classes" numbers in the table above are counts over that half; `killsets.py` counted a class as killed if either of its two colourings was. Uncrossing the dumped three-kill instances at oddness 6 (`tools/uncross3.py`) shows the difference: of the six instances, two have **6 of the 8 colourings** killed (the two survivors are complementary), two have 5, one 4 and one 3. All three tools now count colourings; the tables will be redone in colouring terms.

**Checks and first consequences of the correction.**

- The rule "the path-complement of a killed colouring is killed by $V\setminus S$ iff no even circuit crosses $\partial S$" was verified on the six three-kill instances (`tools/check_complement.py`): no violation, and in the two 6-of-8 instances all witness cuts have $s=0$, so there the four-class picture is exact and three of the four complementary pairs are genuinely dead.
- Per-colouring histogram at oddness 6 (`killsets.py`, cyclically 6-connected `six12`, `straddleA`, `straddleC`, 1,080 instances): killed colourings of 8: $\{0{:}692,\ 1{:}138,\ 2{:}211,\ 3{:}17,\ 4{:}18,\ 5{:}2,\ 6{:}2\}$. Single kills (an 11-cut crossed by an even circuit) are common, so the even-circuit colouring is not a side issue: it decides whether a tight cut kills one colouring or two.
- **The missing pattern is realized with zero slack.** For a surviving colouring one can force a pattern (chosen path ends inside $S$, the others outside) and minimize $3d(S)-5(b_S-a_S)$ by one min cut (`tools/forced_cut.py`). In the 6-of-8 instance `threekill_straddleC_5_0_43` both survivors have forced cuts with **slack 0**: a 15-cut with $k=9$ and a 10-cut with $k=6$, i.e. $3m=5k$ exactly. Bad needs $5k>3m$, so these colourings survive by a single edge. The same happens for one survivor of `six12_5_3_204` (a 10-cut with $k=6$). The $m=5j$, $k=3j$ cuts are the boundary cases of the catalogue (the inequality $5k>3m$ excludes them, and nothing else does), and the data says the fourth pattern of situation 5 tends to be realized precisely by such a boundary cut. A proof that the fourth pattern cannot be *bad* therefore has no room: it must show that the best cut for that pattern has $3m\ge5k$, with equality attainable. `tools/slack_stats.py` measures how often survivors sit at slack 0 (running).

**Slack statistics** (`slack_stats.py`, 720 instances of `straddleC`, `six12`, `straddleA`, 40 0-edge choices per graph): in about 93% of all instances, including those with nothing killed, some surviving colouring has a forced cut of slack exactly 0, realized by the boundary types $(10,6,4,q{=}2\text{ or }4)$, $(10,6,3,3)$ and $(15,9,6,3)$. So $3m=5k$ cuts are the generic situation, not a special feature of the near-failures: the catalogue's strict inequality $5k>3m$ is what separates "always some colouring survives" from failure, by one edge, essentially everywhere. Any proof will have to handle the equality cases as the typical ones rather than as a degenerate corner.

**Per-colouring recount of the table above** (all $2^t$ path colourings, even circuits fixed; `kill_full_colourings.log`):

| oddness $2t$ | colourings | family | instances | with 0 killed | worst kill |
|---|---|---|---|---|---|
| 8 | 16 | random $8C_7{+}C_8{+}C_6$ | 320 | 272 | 4 |
| 8 | 16 | 5-block straddle rings | 320 | 84 | 10 |
| 8 | 16 | 4-block rings $[7,7,6]$ (two seeds) | 640 | 107 | **14** |
| 10 | 32 | 5-block rings $[7,7,6]$ | 160 | 7 | **29** |
| 12 | 64 | 6-block rings $[7,7,6]$ | 160 | 1 | **56** |
| 14 | 128 | 7-block rings $[7,7,6]$ | 160 | 0 | **120** |
| 16 | 256 | 8-block rings $[7,7,6]$ | 160 | 0 | **246** |

**Correction to the status line of §0.** The status line claims "the four-class cover problem is solved for covers by 6-cuts (Lemma 6.5)". The commit that introduced that sentence (37194ae) changed nothing else in these notes, and no Lemma 6.5 was ever written down here. Situation 2 (three pair-type 6-cuts on the three pairs with sign product $+1$) is therefore **open**, not settled; only situation 1 (Lemma 6.4, the MS argument) is proved. In per-colouring terms situation 2 reads: with $c(z_{2i})=-c(z_{2i-1})$ along each path, a pair cut with $s=0$ on $\{a,b\}$ containing ends $z_\alpha\in P_a$, $z_\beta\in P_b$ kills the four colourings with $c(z_\alpha)=c(z_\beta)$; three such cuts kill all eight iff the product of their signs is $+1$. In the census `tools/paircuts.py` (running) I look for two tight pair cuts on different pairs in one instance; in the signature logs so far, none coexist.

**Pair-cut census** (`tools/paircuts.py`, forced pair patterns on `six12` and `straddleA`, 480 instances): tight pair cuts are rare when searched systematically (7 and 9 instances respectively), most are crossed by the third path or by an even circuit (so they kill one or two colourings, not four), a codimension-2 pair cut with $s=0$ occurred once, and two tight pair cuts on *different* pairs coexisted once (both with $s=1$). So situation 2 is empirically remote; the near-failures are made of 11-cuts.

## 8. Directed search and exact search (started 2026-09-12 evening)

Random sampling reaches 6 of 8 killed colourings at oddness 6 at a rate of about 1 in 500 instances. Two stronger tools replace it:

- `tools/climb.py`: hill-climbing with annealing over the whole instance for a fixed planted 2-factor: matching 2-swaps keeping girth $\ge6$ (cyclic 6-edge-connectivity re-checked by SAT on every accepted swap), 0-edge moves and even-component flips; objective = killed colourings, tiebreak = the survivors' forced-cut slack; restarts after 1,500 stale steps; near-misses ($\ge 2^t-2$ killed) are dumped as solver hints. First runs (eight climbers, 20,000 steps): 6 of 8 reached within a few hundred steps on `straddleA` and the random $6C_7{+}C_8$ family, 12 of 16 at oddness 8.
- `tools/fullkill_sat.py`: an exact CP-SAT model for a fixed planted 2-factor. Variables: the perfect matching (girth $\ge6$ by binary clauses), the 0-edge of each odd circuit, the base 2-colouring of $H$, a label in $\{0,1,2,3\}$ per vertex propagated along $H$-edges, integer potentials that increase by exactly one along $H$-edges at nonzero-labelled vertices (this forces the nonzero label classes to be exactly the three $z$–$z$ paths, since an $H$-cycle cannot carry a monotone potential and each label is required to contain exactly two $z$'s), and for each of the $2^t$ path colourings a set $S$ with $5(b_S-a_S)\ge3d(S)+1$, switched on by a "kill" literal. Objective: maximize the number of kills; $2^t$ is a template failure. Cyclic 6-edge-connectivity is added lazily: each solution is checked by SAT and any small cyclic cut $X$ is excluded by the linear constraint $d(X)\ge6$ on the matching variables. A proof of optimum $<2^t$ for a given 2-factor shape would be a computer proof that the template cannot fail on that shape, for every matching at once. Warm starts from dumped near-miss instances are supported.

**Results of the first exact and directed searches.**

- CP-SAT (`fullkill_sat.py`) was validated on the 6-of-8 instance `threekill_straddleC_5_0_43`: with the matching, 0-edges, base colouring and labels fixed, it proves the optimum 6 in 3 seconds once the tight-cut structure of Lemma 6.1 is added as constraints (at most $3t-2$ crossing matching edges, $1$ to $2t-2$ crossing colour-2 edges, at most one other edge, every crossing colour-1 or colour-2 edge black at its $S$-end, $|S|\ge6$). Without those constraints it cannot even find the witness sets. In free search (30 minutes, 4 workers) it reaches 4 of 8 on $6C_7$ ($n=42$) and on the `straddleC` shape, with the upper bound stuck at 8: no proof and weaker than the climber at finding kills. The model is kept as a verifier and for planted-cut experiments (fixed candidate sets), not as a free search.
- The climber reaches 6 of 8 on `straddleA`, `straddleC`, the random family and 13 of 16 at oddness 8, and then stalls; full restarts discarded the good states, replaced now by kicks from the best state.
- **Anatomy of the killing 11-cuts** (`tools/anatomy.py` on the two 6-of-8 instances): every witness is $S=$ (two or three whole circuits of $F_2$, at least one of them odd) $\cup$ (exactly two arcs of two further circuits), $|S|\in\{21,25,27\}$, with exactly 7 matching edges leaving $S$ and the 4 arc ends as the colour-2 boundary edges, one end of each path inside. Crossing profiles $(r_1,r_2,r_3;s)$ of the witnesses across the four three-kill instances: $(7,3,1;0)$, $(5,5,1;0)$, $(5,3,3;0)$, $(5,1,5;0)$, $(1,1,9;0)$, $(3,3,5;0)$, $(5,1,3;1)$, $(1,3,7;0)$, $(3,5,3;0)$, $(3,3,3;1)$: all odd triples summing to $11-2s$ occur, so no profile restriction can be the obstruction.
- Literature check (September 2026): nothing beyond Mazzuoccolo–Steffen (oddness 4), Kochol's reduction and Steffen's cyclic-connectivity bound; oddness 6 is open and no published example of the template failing exists.

## 9. The end graph: survivability as a not-all-equal problem

Let $Z=\{z_1,\dots,z_{2t}\}$ and write $c(z)\in\{B,W\}$ for the colour of an end under a path colouring. Along a path the two ends always have opposite colours (paths have an even number of vertices), so a path colouring is the same thing as an assignment $c\colon Z\to\{B,W\}$ with $c(z_{2i})\ne c(z_{2i-1})$, i.e. $t$ free bits. Every tight cut $S$ acts on the ends only through $S\cap Z$ (Lemma 6.1: $k=c_2+q$ is tight, so the ends inside $S$ on separated paths are all black, and complementing $S$ makes them all white): a tight cut with $S\cap Z=T$ and $|J^*(S)|=|T|$ kills exactly the colourings in which **$T$ is monochromatic**. Hence, for a fixed instance, the set of killed colourings is the set of assignments violating one of the constraints

- $c(z_{2i-1})\neq c(z_{2i})$ for each path (always present),
- $\mathrm{NAE}(T)$ for each set $T=S\cap Z$ of a tight cut $S$ whose separated paths are exactly the paths met by $T$ (a pair cut gives a 2-clause, i.e. $c(z_\alpha)\ne c(z_\beta)$; an 11- or 7-cut a 3-clause),

and the template fails iff this **not-all-equal instance on $Z$ is unsatisfiable**. (Cuts crossed by paths they do not separate kill fewer colourings; cuts with $s>0$ kill only the black version, so they are "half" clauses; both only make failure harder.)

Consequences.

1. **Pair cuts alone (2-clauses).** The constraint graph on $Z$ (path edges plus pair-region edges, all "$\neq$") must be non-bipartite for a failure. Its odd cycles are exactly: two pair regions on the same pair of paths with opposite signs (a triangle through a path edge: situation 1, excluded by Lemma 6.4) and, at $t=3$, three pair regions on the three pairs with sign product $+1$ (situation 2, a triangle or a 5-cycle through two path edges). A cycle that alternates path edges and region edges has even length, which is why the rings of blocks with two ends per block can never fail through their block cuts.
2. **Rings explained.** In the 4-block ring instance with 14 of 16 killed, the blocks carry ends $\{z_1,z_3\},\{z_3',z_5\},\dots$ and the paths join the blocks in a 4-cycle; the two survivors are exactly the two colourings that give every block one black and one white end, i.e. the two orientations of that 4-cycle of paths. In general, orienting each cycle of the "path graph on blocks" consistently gives block-balanced colourings, and block-balanced colourings survive all block cuts because their $z$-imbalance is $0$ there. This is the mechanism behind the $2^t-2$ ceilings observed at every oddness on rings.
3. **11-cuts (3-clauses).** At $t=3$ the three free bits $c(z_1),c(z_3),c(z_5)$ admit 8 assignments, NAE-clauses are complement-symmetric, so each 3-clause kills one of the 4 classes and a failure by 11-cuts alone needs **four tight 11-cut regions $T_1,\dots,T_4$ whose monochromatic patterns are the four classes**; up to renaming, all four regions contain $z_1$ and they realize all combinations $\{z_3\text{ or }z_4\}\times\{z_5\text{ or }z_6\}$. The data: three of the four coexist (three instances in 1,080; two of them with all three cuts having $s=0$), never four. So the theorem to prove at oddness 6 is:

> **(T4)** In a cyclically 6-edge-connected cubic graph with the canonical colouring of a 6-odd 2-factor, there are no four tight 11-cuts $S_1,\dots,S_4$ with $S_i\cap Z$ running through the four classes of triples (together with the mixed cases: two 11-cuts plus one pair cut, one 11-cut plus a 3-class pair configuration, and the 7-cut and $(6,4,1,3)$ variants).

The parity tool of MS applies to every set $U$ whose boundary lies in $E(H)$: $|\partial U|\equiv$ number of paths separated by $U \pmod 2$ (every path crosses $\partial U$ an odd number of times iff separated, even circuits of $H$ cross evenly). For two tight 11-cuts with $s=0$ realizing adjacent classes this gives, with $A=S\cap S'$, $B=V\setminus(S\cup S')$, $C=S\setminus S'$, $D=S'\setminus S$: $d(A),d(B)$ even, $d(C),d(D)$ odd, $AD+BC=AC+BD$, and $AB+CD+(AD+BC)=11$; the colour conditions force every $A$–$B$ edge onto $P_1\cup P_2$ and every $C$–$D$ edge onto $P_3$. These are consistent for two cuts (as observed); (T4) needs the same bookkeeping on the 16 atoms of four cuts, which I have not done.

**Computer-assisted attack on (T4), pure case** (`tools/t4_abstract.py`). For four tight 11-cuts of type $(11,7,4,3)$ with $s=0$ realizing the four classes, every edge between two of the 16 atoms is an $H$-edge on a path, so each path is a walk through atoms with segments (atom, length). The model has: segments with lengths and position parities; colours alternating $1,2,1,\dots$ from each $z$; the black-end rule at every crossing of every $S_j$ under its pattern colouring; $7$ colour-1 and $4$ colour-2 crossings per cut with per-path crossing numbers odd; per-atom parities ($n_a\equiv z_a$, colour-1 crossings at $a$ $\equiv z_a$, colour-2 crossings at $a$ even, from the three perfect matchings colour 1, colour 2 $\cup$ $\{z\}$, colours 0/3); and for atom unions $U$ (at most two atoms) the cubic and connectivity facts $d(U)\le3n(U)$, $n(U)\le5\Rightarrow d(U)\ge n(U)+2$ (girth 6), $n(U)\ge1\Rightarrow d(U)\ge3$, $d(U)\le5\Rightarrow n(U)\le3$ (cyclic 6-edge-connectivity; the complement holds at least four $z$'s). All of these are necessary conditions, so infeasibility is a proof. **Result: with at most 8 segments per path the system is infeasible (131 s).** A complete proof needs up to 37 segments per path (a path crosses each cut at most 9 times); the model with 12 segments was not decided in 500 s before the crossing-count structure was added. Extension in progress.

**Blueprint A is impossible, by hand.** Its cut $S_4$ consists of three 3-vertex $z$-atoms $\{u,z,w\}$ ($u$ across the 0-edge, $w$ across the colour-3 edge), 9 vertices with $d=11$, hence 8 internal edges: the six $z$-edges and two $H$-edges between the atoms. Tightness ($k=7$ on 9 vertices) allows only one white vertex, so the two internal $H$-edges must share their white end, i.e. some $u_1$ has its colour-1 edge to one atom and its colour-2 edge to another. But every non-$z$ vertex of a 3-vertex $z$-atom is a circuit neighbour of its $z$, so a colour-2 edge between two such atoms puts two $z$'s on one circuit of $F_2$, which the canonical colouring forbids. Two internal colour-1 edges give two white vertices ($k\le5$), and zero internal $H$-edges give $d=15$. So the relaxation misses the circuit structure. Adding the odd circuits as closed walks through the atoms (colour-2 crossings partitioned among circuit classes, even degree per atom, room $c_2(a)+z(a)\le n(a)$, a circuit must leave an atom of fewer than 7 vertices, a 3-vertex $z$-atom is traversed only by its own circuit) the relaxation is **still feasible** (blueprint C: atoms 0000:3, 0011:3, 0101:7, 1010:7, 1100:7, 1111:15, $N=42$). The abstraction adapts to every fact added; it will not deliver (T4). Its remaining use is to propose blueprints for the realization model, which is running on C.

## 10. Computer-verified exclusions in the cover problem (oddness 6, pure case)

The count relaxation was generalized to arbitrary configurations of tight cuts with $s=0$ (`tools/count_general.py`, `tools/enum_covers.py`): $J$ cuts, $2^J$ atoms, one walk per path with forced crossing colours, the cut counts $(c_1,c_2)$, per-path odd crossing numbers, atom parities, circuit classes, $N\ge42$, and the cubic/girth/connectivity facts for atom unions (lazily for all $2^J$ unions), plus, in the latest version, walk connectivity for paths and odd circuits. A pair cut of codimension 2 is modelled with the avoided path entirely on one side ("placement"), which the model distinguishes. Every constraint is a necessary condition, so INFEASIBLE is a proof. Validation: a single pair cut and the two-cut placement observed in the `straddle6A` near miss are feasible; the Mazzuoccolo–Steffen situation 1 (same pair, opposite signs) is infeasible in all 12 placements, reproducing Lemma 6.4.

Enumeration of all **minimal covers of the four classes** by lines (pair 6-cuts) and points (11-cuts or 7-cuts), 204 configurations:

| situation | configurations | infeasible | feasible relaxation |
|---|---|---|---|
| 1: two parallel lines | 12 | 12 | 0 |
| 2: three lines, one per pair | 32 | **32** | 0 |
| 3: two lines + one point | 96 | 72 | 24 (all with an 11-cut point and the two avoided paths on opposite sides) |
| 4: one line + two points | 48 | 12 (both points 7-cuts) | 36 |
| 5: four points | 16 | 1 (four 7-cuts) | 9 feasible (all with at least two 11-cuts), 6 undecided at 2 minutes (7-heavy mixtures) |

**Lemma 10.1 (situation 2 is impossible, pure case).** Three tight pair cuts of type $(6,4,2,2)$ with $s=0$, each avoided by its third path, whose end pairs form a triangle, cannot coexist in a cyclically 6-edge-connected cubic graph with the canonical colouring. (Computer proof: all 32 placements infeasible.)

**Lemma 10.2.** In situation 3 the point-killer cannot be a 7-cut; with an 11-cut the two avoided paths must lie on opposite sides of their pair cuts (one inside, one outside). In situation 4 the two point-killers cannot both be 7-cuts.

What remains open at the class level, with $s=0$: situation 3 with an 11-cut and mixed placement, situation 4 with at least one 11-cut, and situation 5 (four 11-cuts, or 11/7 mixtures). These are exactly the configurations whose relaxations are feasible, so any proof of them needs information beyond atom counts and parities (the order of crossings along the paths, or the geometry of the circuits). The near misses found by the climbers are of these types: three 11-cuts (situation 5 minus one), and two pair cuts with opposite placements plus an 11-cut (situation 3 minus nothing: the `straddle6A` instance has all three cuts but its third pair cut is missing, i.e. it is a 3-kill of type situation 3 that would need a fourth cut).

**What the proofs use (ablation).** Re-solving the infeasible configurations with constraint groups removed (circuit classes; the cubic/girth/cyclic-connectivity facts on atom unions, including the lazily generated ones):

| configuration | full | without circuits | without union facts | without both |
|---|---|---|---|---|
| situation 1 (MS), placement 00 | infeasible | feasible | feasible | feasible |
| situation 2, placement 000 | infeasible | infeasible | feasible | feasible |
| situation 2, placement 111 | infeasible | infeasible | feasible | feasible |
| situation 2, b01 s1 placement 010 | infeasible | feasible | feasible | feasible |

So the union facts (cyclic 6-edge-connectivity and girth applied to unions of atoms, the setting of the MS uncrossing) are needed in every proof, and the circuit structure of the 2-factor (each odd circuit is a closed walk through the atoms leaving any atom of fewer than 7 vertices, a 3-vertex $z$-atom is traversed only by its own circuit) is needed for situation 1 and for some placements of situation 2. The two "symmetric" placements of situation 2 (all avoided paths outside, or all inside) fall to uncrossing and counting alone, and are the natural first targets for a written proof. In the model, MS's Lemma 6.4 is not reproduced by their parity argument but by the circuit facts; whether the parity argument is implicitly present in the union facts I have not checked.

**Lemma 10.3 (situation 2, symmetric placements, by hand).** Let $S_{12},S_{13},S_{23}$ be tight pair cuts of type $(6,4,2,2)$ with $s=0$, $S_{12}\cap Z=\{z_1,z_3\}$, $S_{13}\cap Z=\{z_1,z_5\}$, $S_{23}\cap Z=\{z_3,z_5\}$, and suppose each cut has its third path entirely outside ($P_3\cap S_{12}=P_2\cap S_{13}=P_1\cap S_{23}=\emptyset$). Then $G$ is not cyclically 6-edge-connected. (The all-inside placement is the same statement after complementing the three cuts and renaming ends.)

*Proof.* Write atoms by membership $(S_{12},S_{13},S_{23})$. Every edge between two atoms lies in some $\partial S_{ij}\subseteq E(H)$ and, as no even circuit of $H$ crosses, is a path edge. $P_1$ avoids $S_{23}$, so its vertices lie in atoms with third coordinate 0; likewise $P_2$ has second coordinate 0 and $P_3$ first coordinate 0. Atom 111 carries no path vertex, hence no boundary edge, hence is empty. Put $A=S_{12}\cap S_{13}=110\ni z_1$, $B=S_{12}\setminus S_{13}=100\cup101\ni z_3$, $C=S_{13}\setminus S_{12}=010\cup011\ni z_5$, $D=000\cup001\ni z_2,z_4,z_6$. Each $z$ keeps its two non-$H$ neighbours in its own atom (non-$H$ edges never join atoms), so $n(A),n(B),n(C)\ge3$ and $n(D)\ge9$. Boundaries of the four parts lie in $E(H)$, and each part separates exactly one path ($A$: $P_1$; $B$: $P_2$; $C$: $P_3$; $D$: all three), so $d(A),d(B),d(C),d(D)$ are odd. Cyclic 6-edge-connectivity gives $d(X)\ge6$ unless $X$ is acyclic with at most 3 vertices, and $d(X)=n(X)+2$ for such $X$; hence each of $d(A),d(B),d(C)$ is $5$ (the part being exactly $\{u,z,w\}$, the $z$ with its two non-$H$ neighbours) or $\ge7$, and $d(D)\ge7$. Submodularity gives $d(A)+d(D)\le d(S_{12})+d(S_{13})=12$ and $d(B)+d(C)\le12$. Therefore $d(A)=5$, $A=\{u_1,z_1,w_1\}$, and $\{d(B),d(C)\}$ is $\{5,5\}$, $\{5,7\}$ or $\{7,5\}$.

No edge joins $B$ to $C$: the pairs $(100,011),(101,010),(101,011)$ share no path, and an edge $100\to010$ would leave $S_{12}$ and enter $S_{13}$ on $P_1$, which forces colour 1 and colour 2 at once (Lemma 6.2 with $z_1$ in both cuts). Likewise $e(110,101)=e(110,011)=e(110,001)=0$. With $a=e(110,100)$, $b=e(110,010)$, $c=e(110,000)$, $p=e(B,D)$, $q=e(C,D)$: $a+b+c=d(A)=5$, $b+c+p=d(S_{12})=6$, $a+c+q=d(S_{13})=6$, so $p=a+1$, $q=b+1$, $d(B)=a+p=2a+1$, $d(C)=2b+1$. If $d(B)=5$ then $B=\{u_3,z_3,w_3\}$ lies in the atom 101 of $z_3$, atom 100 is empty, $a=0$ and $d(B)=1$, a contradiction; so $d(B)=7$, $d(C)=5$, and symmetrically atom 010 is empty, $b=0$, $d(C)=1$, a contradiction. $\square$

This is exactly the computation the solver performs; the other placements of situation 2 additionally use that the odd circuit through a $z$ in a 3-vertex atom must leave that atom through both of its non-$z$ vertices, which forbids colour-2 edges between two such atoms.

**Corrected ablation over all 129 infeasible configurations** (`enum_covers_v2.log`): without the union facts every one of them becomes feasible; without the circuit facts 33 stay infeasible (8 of the 32 placements of situation 2, 24 of the 72 infeasible situation-3 configurations, and the four-7-cut cover) and 96 become feasible. So the union facts (cyclic 6-edge-connectivity and girth on atom unions) carry every proof, and the circuit facts are needed for situation 1, for 24 placements of situation 2, for the other 48 situation-3 exclusions and for the 12 situation-4 exclusions. **Lemma 10.4.** Four tight 7-cuts of type $(7,5,2,3)$ with $s=0$ cannot realize the four classes.

## 11. State of play (end of 2026-09-12)

**What is proved.** In the pure setting (tight cuts with $s=0$; pair cuts of codimension 2; cyclically 6-edge-connected cubic graph with the canonical colouring of a 6-odd 2-factor), the four colouring classes cannot be covered by: two parallel pair cuts (Lemma 6.4, MS, reproduced); three pair cuts (Lemma 10.1, computer; Lemma 10.3 by hand for the symmetric placements); two pair cuts and a 7-cut, or two pair cuts and an 11-cut with both avoided paths on the same side (Lemma 10.2); one pair cut and two 7-cuts (Lemma 10.2); four 7-cuts (Lemma 10.4). Every one of these proofs runs on the cyclic-connectivity and girth facts for unions of atoms; most also use that the odd circuit through a $z$ leaves a small $z$-atom through both non-$z$ vertices.

**What is open.** The covers that survive the atom relaxation: situation 3 with an 11-cut and the two avoided paths on opposite sides; situation 4 with at least one 11-cut; situation 5 with at least two 11-cuts (7-heavy mixtures undecided). Their relaxations are feasible even with circuit walks and walk connectivity added, so no argument at the level of atoms, crossing counts and parities can exclude them: a proof must use the order of crossings along the paths or the vertex-level structure of the circuits. The realization of two blueprints as actual graphs was undecided by CP-SAT after two hours each. The climbers reach the corresponding near misses within seconds (three 11-cuts; two pair cuts with opposite placements plus an 11-cut) and never complete them: about 100,000 evaluated states at oddness 6 with no full kill.

**What would decide it.** Either a vertex-level exact model that keeps the cut sets fixed to a blueprint and scales (the current realization model has too much symmetry on 40 to 60 free vertices), or a lemma about two 11-cuts whose patterns differ on one path that uses the crossing order (the local lemma of §6 is the atom-level shadow of it, and it is consistent). If (O6) is false for this template, the counterexample lives in exactly these three configurations, and the straddle rings are the place to look.

*Addendum (2026-09-13).* The six situation-5 mixtures with two or more 7-cuts remained undecided by the relaxation after 15 minutes each with 3-atom unions (`enum_s5_long.log`); the climbers on `t4ring`, `rand9` and `straddle6A` ended their runs without a full kill (best 14 of 16 and 6 of 8).

**Structured slack and completion search (2026-09-13).** For a surviving colouring of a 6-of-8 instance, the best cut of the observed 11-cut shape (one whole $z$-circuit, arcs around the other two required ends, any even circuits) has $3d-5k=10$ on `threekill_straddleC_5_0_43` ($d=15$, $k=7$) and $12$ to $16$ on the `straddle6A` near miss (`tools/structured_slack.py`): four to six boundary matching edges too many. Unlike the generic forced slack (zero everywhere), this is a usable gradient. `tools/complete.py` anneals over matching 2-swaps from a near-miss state, keeping its 2-factor, 0-edges and even colouring, with objective (killed colourings, minus the worst structured slack over survivors); a negative structured slack for a survivor is a full kill. Four runs of 2,000 steps started on the dumped 6-of-8 states. The exact model with the three known cuts fixed and a structured fourth cut (`S4SPEC` in `fullkill_sat.py`) was undecided in 2 minutes.

## 12. A counterexample to the fixed-2-factor template at oddness 6 (2026-09-13)

**The instance** (`data/oddness6_template_counterexample.json`, edge list with colours in `data/oddness6_template_counterexample_edges.txt`; found by `tools/complete.py` in 135 annealing steps from the 6-of-8 climber state `best_climb_straddleC_22.json`; verified independently by `tools/verify_fullkill.py`):

- a cubic graph $G$ on 60 vertices, girth 6, 3-edge-connected, **cyclically 6-edge-connected** (SAT: no cyclic cut of size $\le5$); it has a nowhere-zero 5-flow (SAT);
- a 2-factor $F_2$ with circuits of lengths $7,6,7,6,7,7,6,7,7$ (six odd circuits, oddness 6 for this 2-factor) and a 0-edge on each odd circuit;
- the canonical colouring gives an $H$ that consists of **exactly three paths** $P_1=z_{59}\dots z_{28}$, $P_2=z_{47}\dots z_{19}$, $P_3=z_{4}\dots z_{37}$ and **no even circuit**;
- **all eight 2-colourings of $H$ are unbalanced.** Each is killed by a tight 11-cut of type $(11,7,4,3)$ with $5|b_S-a_S|-3|\partial S|=2$.

Since $H$ has no even circuits, the eight path colourings are all the colourings the method offers for this $(F_2,c)$: the Mazzuoccolo–Steffen construction fails here with the 2-factor and the 0-edges fixed, not merely the path-recolouring template. In the language of §9 this is situation 5 realized: four 11-cuts $S$ (with their complements) whose $z$-triples are the four classes. Their structure is simple. Let $C_4$ be the odd circuit through $z_{28}$. The four small sides are
$$S=C_4\ \cup\ \mathrm{arc}(z_{19}\text{ or }z_{47})\ \cup\ \mathrm{arc}(z_{4}\text{ or }z_{37}),$$
a whole circuit plus an arc of 3 or 5 vertices around one end of $P_2$ and one end of $P_3$, with $z_{59}$ always outside; the sides have 13, 13, 15 and 17 vertices, 7 matching edges and the 4 arc ends leaving. So statement (T4) of §9 is **false**, and so is the hope, expressed in §5 and §6, that the five bad-cut types cannot jointly kill all classes on a cyclically 6-edge-connected graph.

**What it does not show.** It is not a counterexample to (O6) or to the 5-flow conjecture: the graph has a 5-flow, and the method still succeeds on the same 2-factor with other 0-edge choices (300 random choices out of $7^6$ all admit a balanced colouring; the full sweep is running). The 0-edge freedom, which Mazzuoccolo–Steffen never need at oddness 4 (there the fixed template always works), is therefore **essential** at oddness 6. Any proof of (O6) along these lines must choose the 0-edges (or the 2-factor) as a function of the graph, and the proof of §6's plan (a) is dead.

**Consequences for the computer-verified lemmas of §10.** They remain true: they concern situations 1–4 and the 7-cut variants, and situation 5 with 11-cuts was exactly the case whose relaxation was feasible. The relaxation was right to be feasible.

**Two more instances (same morning).** Rerunning `complete.py` from the other dumped 6-of-8 states gave, within 84 and 160 steps, `data/oddness6_template_counterexample_n50.json` (50 vertices, circuits $7,7,7,7,7,7,8$; the smallest possible order for that profile) and `data/oddness6_template_counterexample_n60b.json` (60 vertices; its four killing cuts each have exactly 13 vertices: the whole odd circuit through one path end plus two 3-vertex arcs around one end of each other path). Both verified as above; in both, $H$ is three paths with no even circuit. So these failures are not rare accidents of one graph: they appear as soon as the search is pointed at the structured 11-cut slack. Next: the 42-vertex family (six 7-circuits, no even circuit at all) and the 0-edge-robust question (does a 2-factor exist for which every 0-edge choice fails?).

**The 0-edge sweep.** For the first counterexample's graph and 2-factor, all $7^6=117{,}649$ choices of 0-edges were tested with all $2^{\#\text{components}}$ colourings of $H$ (`tools/zero_sweep.py`, `zero_sweep_all.log`): **exactly one choice fails**, the counterexample's own $(4,6,2,4,1,6)$; every other choice admits a balanced colouring. So for this 2-factor the method's failure is an isolated point of the 0-edge space, and moving a single 0-edge repairs it. Whether a 2-factor exists for which a positive fraction, or all, of the 0-edge choices fail is the question the 0-edge-robust climber (`tools/robust.py`) is after; from the counterexample it has not increased the count beyond 1 of 40 sampled choices in its first steps.

**Fourth instance.** `data/oddness6_template_counterexample_n54.json` (54 vertices, circuits $7,6,7,7,7,6,7,7$, from the `straddleA` near miss, 110 completion steps), verified. Four families, four instances; the completion search succeeds in about one run in three.
Its killers are two pair-type 6-cuts $(6,4,2,2)$ that are **crossed by their third path** (5 of 8 and 15 of 18 vertices of the avoided path inside), so each kills a single class, together with two 11-cuts: a situation-5 cover in which two of the four points are codimension-3 pair cuts. This variant was not in the enumeration of §10 (which models pair cuts only as codimension-2 lines), so no lemma there is contradicted; it shows that the codimension-3 pair cuts, dismissed in Corollary 6.3 as "killing only one class", are enough to complete a cover.

**Fifth instance, 42 vertices (the minimum).** `data/oddness6_template_counterexample_n42.json`: six 7-circuits and nothing else ($n=42$ is the least order at which a girth-6 cubic graph can have a 2-factor of six odd circuits), cyclically 6-edge-connected, $H$ = three paths, all eight colourings killed by 11-cuts of 13, 13, 13 and 15 vertices. Found directly by `climb.py` (profile `six7`, seed 43, step 1905) without the completion stage, verified as before; the graph has a nowhere-zero 5-flow. The instance is written out in `data/README.md`.

## 13. After the counterexamples: what (O6) now needs

The fixed template is dead, but the sweeps say something positive: for the 60-vertex instance exactly one of $7^6$ 0-edge choices fails, and the same sweep is running for the 42-vertex one. The failures are isolated points. This suggests replacing plan (a) of §5 by an **averaging argument over 0-edges**:

> **(O6′)** For every cyclically 6-edge-connected cubic graph with a 2-factor of six odd circuits there is a choice of 0-edges (one per odd circuit) and a 2-colouring of $H$ that is balanced.

A proof would count, for a fixed 2-factor, the pairs (0-edge choice, colouring class) killed by tight cuts and show that fewer than $7^6\cdot 4$ (with 7-circuits) can be killed. What is known about a single tight 11-cut $S$ is favourable: its four colour-2 boundary edges are the ends of two arcs, so $S$ depends on the 0-edges of the two arc circuits only through the colours of those four edges, and moving a 0-edge along an arc circuit by one position flips the colours of all its circuit edges, which destroys the tightness of every 11-cut using an arc of that circuit (the arc ends become colour 3 and leave $H$). Hence each tight 11-cut is tied to the 0-edge positions of its two arc circuits modulo the alternation, i.e. to a set of 0-edge choices of density at most $1/4$ in the two coordinates concerned. Killing all four classes needs four such cuts using at least two distinct arc circuits in total; the question is whether the union of the corresponding regions of the 0-edge space can be everything, and the data (one point in $117{,}649$) says it is far from it. A counting proof along these lines would also have to handle the pair cuts and 7-cuts and the way $H$ itself changes with the 0-edges (the paths are re-routed, since which circuit edges are colour 2 changes). This is the plan I would pursue next; the tools to test any intermediate claim exist (`zero_sweep.py` for the density of failures over 0-edges, `killsets.py`/`killsets_t.py` for the killed sets, `count_general.py` for configuration exclusions).

A second consequence concerns the method's other freedom. The 0-edge-robust search (`robust.py`) tries to make many 0-edge choices fail at once for one 2-factor; if it stays at one failing choice, (O6′) is the right statement; if it produces a 2-factor with all $7^6$ choices failing, the 2-factor itself must be chosen, and the oddness-6 case moves to the level of Steffen's 2-factor arguments.

*Sweep of the 42-vertex instance.* Again exactly one of the $7^6$ 0-edge choices fails, the instance's own $(4,1,6,4,2,2)$ (`zero_sweep_n42.log`). Two out of two 2-factors with a failing choice have precisely one failing choice; the density of failures over the 0-edge space is $1/117{,}649$ in both. A sample of 5,000 choices is being classified by the number of killed colourings to see how the near-failures are distributed around the failing point.
Result (`zero_killdist_n42.log`, 5,000 random choices): $H$ has 3 components in 71% of the choices, 4 in 27%, 5 or 6 in the rest; with three components the killed counts are 0 (3,137), 2 (380), 4 (14) and never 6; with four components (16 colourings) at most 6 killed once; with five (32 colourings) at most 8. So around the failing point the landscape is flat: near-failures at oddness 6 are rare even for a 2-factor that has an exact failure, and the failing 0-edge choice is an isolated point in every sense measured. For the averaging plan of §13 this is the best possible news; for a 0-edge-robust counterexample it is the worst, and the robust climber (`robust.py`, two runs of 1,500 steps from the 60-vertex instance) indeed never raised the number of failing sampled choices above one.

**Two-point robustness test** (`tools/robust2.py`). Keeping the known failing 0-edge choice $c^*$ as a hard constraint, anneal the matching to make the neighbouring choice $c'$ (one 0-edge moved by one step on circuit $i$) fail as well, with the structured-slack gradient under $c'$. Under $c'$ the moved circuit's colours flip, $H$ is re-routed and typically gains a component (16 colourings); at the start 4 of 16 are killed. Runs for all six circuits of the 42-vertex instance and two circuits of the 60-vertex one are in progress; a success would show that failures can cluster and would be the seed of a 0-edge-robust counterexample, a uniform failure would support (O6′).
Results (all six neighbours of the 42-vertex instance and two of the 60-vertex one, 3,000 steps each, and 60,000 steps for the two closest neighbours): **no second failing choice**; the best neighbours reach 14 of 16 killed colourings while the original choice keeps failing. Almost every matching swap destroys the original failure. In graph space the failure is less isolated: of the 148 girth-preserving single matching swaps of the 42-vertex instance, 9 keep all eight colourings unbalanced and keep cyclic 6-edge-connectivity, so the counterexample sits in a small family of neighbouring counterexamples, each still failing for its single 0-edge choice. Taken together: failures are robust under some changes of the graph and fragile under every change of the 0-edges, which is what an averaging argument over 0-edges would want.

**Density of the killers over the 0-edge space** (`tools/cut_density.py`, 42-vertex instance, all $7^6$ choices). For a fixed vertex set $S$, "$S$ is bad for some colouring of $H(c)$" is decided by summing, over the components of $H(c)$, the absolute imbalance of the component inside $S$. The eight witness sets are live for $0.02\%$ to $0.22\%$ of the choices each (24 to 260 choices out of 117,649); two, four, six and eight of them are live simultaneously for 329, 16, 1 and 1 choices, the last being the failing choice itself. The easy part of these densities is combinatorial: a 13-vertex killer is a whole circuit plus two 3-vertex arcs, and both ends of a 3-arc are colour 2 only when the arc is centred at the circuit's $z$, so the two arc circuits' 0-edges are pinned (density $1/49\approx2\%$); the remaining factor of 50 to 100 comes from the routing of $H$ through the other four circuits, which must place the three ends inside $S$ with the right colours. A proof of (O6′) by averaging must capture this second factor, since the number of candidate 11-cut sets (choice of whole circuit, two arc circuits and arc extents) is in the thousands and a union bound with density $1/49$ alone is far from sufficient.

**Where the density comes from.** Let $S$ be a 13-vertex killer (whole circuit plus two 3-arcs) with its three $z$'s inside. Degrees in $H$: the 13 vertices have $2\cdot13-3=23$ $H$-half-edges, 11 of which cross $\partial S$, so $S$ contains exactly 6 internal $H$-edges and hence exactly $13-6=7$ segments of $H$. The maximum of $|b_S-a_S|$ over all colourings is the sum over components of $|{\rm imbalance}|$, and a segment contributes at most 1, with equality iff its length is odd. Tightness needs 7, so **all seven segments have odd length and, under the killing colouring, the same sign**; segments of the same path must therefore sit at mutually compatible parities of position along the path. Two of the three coordinates of the 0-edge choice are pinned by the arc centres (factor $1/49$); the remaining four coordinates re-route $H$ and decide the parities of the $7-3=4$ non-$z$ segments relative to their $z$'s, each roughly a coin flip: $1/49\cdot2^{-4}\approx1.3\cdot10^{-3}$, which is the observed order ($2\cdot10^{-4}$ to $2\cdot10^{-3}$). A proof of (O6′) by averaging would have to make the coin flips rigorous (the parities are determined by how the paths thread the four free circuits, and moving a free 0-edge by one step flips the colour class of that circuit's edges, which reroutes $H$ there), and then control the union over the few thousand candidate sets. This is the precise shape of the remaining problem.

**Checks.** (i) Pinning is exact: for each 13-vertex killer of the 42-vertex instance, all live 0-edge choices (48, 36, 20 out of 117,649) have the two arc circuits' 0-edges at the arc centres; the 15-vertex killer (a 5-arc) allows two positions on the 5-arc, as the colour-2 condition predicts. Conditional on the pinning, 0.8% to 2% of the $7^4$ free choices make the cut live, below the $2^{-4}$ heuristic, so the parity constraints are if anything stronger than independent coin flips. (ii) The other four instances (`cut_density_all.log`): per-11-cut densities $0.03\%$ to $0.6\%$; the codimension-3 pair 6-cuts of the 54-vertex instance are live for up to $2.4\%$; all eight witness sets are live simultaneously for 2 to 4 choices per instance, but only one of those choices actually fails (a cut being bad for *some* colouring is weaker than being bad for the colourings that the other cuts leave alive).

**The lemma to prove (L-avg).** For a cyclically 6-edge-connected cubic graph with a 2-factor of six odd circuits: for every vertex set $S$ that can be a tight cut, the number of 0-edge choices for which $S$ is bad for some colouring of $H$ is at most a fraction $\rho(S)$ of all choices, with $\sum_S\rho(S)<1$ over all candidate $S$ (or a refined count in which the four classes are tracked separately). The pinning gives $\rho\le 1/49$ for two-arc 11-cuts; the data says the truth is 50 to 100 times smaller because of the parities of the segments inside $S$ along the three paths, and this is where the proof would have to work.

**The union bound, computed** (`tools/avg_bound.py`, 42-vertex instance). Candidate tight-cut sets are unions of whole circuits and at most two arcs with the exact matching-edge count of their type; there are 42 pair-type, 44 seven-type and 10,018 eleven-type candidates, of which 1,100 eleven-cuts (and no 6- or 7-cuts) admit any 0-edge choice with the right arc-end colours and three $z$'s inside. Summing those admissible counts gives $B=5{,}138{,}140\approx 10.9\cdot 4N$. So the combinatorial densities alone (pinning, $z$-count) are an order of magnitude short, and the routing factor is essential to an averaging proof; with the measured factor of 50 to 100 the true total would be well below $4N$, which `tools/avg_true.py` estimates by sampling.

**The true total closes with room.** Sampling 600 of the $7^6$ choices on the 42-vertex instance and testing all 10,104 candidate sets at each (`avg_true.py`): the estimated total is $T=\sum_S \mathrm{live}(S)\,k(S)\approx 27{,}600$, i.e. $T/4N\approx0.06$. Per sampled choice the number of live candidate sets is 0 for 87% of the choices, 1 or 2 for 12%, and at most 6. So the averaging inequality holds for this instance by a factor of about 17 once the routing factor is included, even though the instance does fail at one point. The lemma to aim for becomes quantitative and clean:

> **(L-avg′)** For a cyclically 6-edge-connected cubic graph with a 2-factor of six odd circuits, the expected number, over a uniformly random choice of 0-edges, of tight-cut candidate sets that are bad for some colouring of $H$, weighted by the classes they kill, is less than 4.

The data gives about $0.24$ for this expectation on the 42-vertex instance. Since a failing choice needs at least three live cuts with complementary patterns, even the weaker statement "the expected number is less than 4" is far from tight, and any proof that captures a constant fraction of the parity constraints along the paths would suffice.

**On all five instances** (`avg_true_all.log`, 400 sampled choices each): $T/4N = 0.013,\ 0.028,\ 0.062,\ 0.062,\ 0.223$ for the 60-, 60b-, 50-, 42- and 54-vertex instances. The averaging inequality holds with a factor of 4 to 75 on every counterexample, i.e. on the 2-factors most hostile to it that we know.

**The routing factor is a product over circuits.** For the 13-vertex killers of the 42-vertex instance the live 0-edge choices form near-exact product sets: killer 1 has three coordinates pinned to one position and the other three restricted to 3, 4 and 4 of the 7 positions ($1\cdot1\cdot1\cdot3\cdot4\cdot4=48$ live choices); killer 3 likewise ($5\cdot1\cdot4\cdot1\cdot1\cdot1=20$); the 15-vertex killer is close to a product with sets of sizes 4, 1, 5, 2, 4, 4. The mechanism is local: when a path of $H$ passes through a circuit $C$ it enters at a vertex $u$ by its matching edge, follows $u$'s colour-2 circuit edge to a neighbour $w$, and leaves by $w$'s matching edge; which neighbour is $w$ depends on the parity of the 0-edge position of $C$, so moving that 0-edge by one step reroutes every path through $C$, and it also moves $C$'s own path end $z$. Hence, per free circuit, only a subset of the positions keeps a given $S$ live, and the subsets multiply. A proof of (L-avg′) could therefore proceed circuit by circuit:

> **(L-circ)** For a candidate tight set $S$ and a circuit $C$ disjoint from $S$ that carries a path segment relevant to the tightness of $S$, at most $\lceil |C|/2\rceil$ positions of $C$'s 0-edge keep $S$ bad for some colouring.

Two such circuits with the arc pinning $1/49$ already give density $\le (4/7)^2/49\approx0.7\%$ per 11-cut, against the $B/4N\approx11$ union bound, which would bring the total below $4N$ for the 42-vertex instance; the data suggests the truth is stronger still. This is where I would start the written proof.

## 14. Resume checklist (paused 2026-09-13)

**State.** Five verified counterexamples to the fixed-2-factor template (§12, `data/`, `data/README.md`); computer-verified exclusions §10; the averaging plan §13 with (L-avg′) and (L-circ). All tools in `tools/`, all logs (`*.log`, gitignored) in `tools/`.

**Running when paused** (single-threaded, they finish on their own):
- `tools/lcirc_n42.log`: `lcirc_test.py` on the 42-vertex instance, all 117,649 0-edge choices × all 10,104 candidate sets; ends with the lines "candidates with live > 0", "marginal sizes over circuits DISJOINT from S", "marginal sizes over circuits meeting S". (L-circ) predicts that the marginal sizes over disjoint circuits are at most 4 (of 7) whenever the circuit matters; a count of 7 means the circuit is irrelevant to that set, which is allowed. Look at the "examples of disjoint circuits with all 7 positions live" line.
- `tools/lcirc_n50.log`: same for the 50-vertex instance.

**To pick up.**
1. Read the two logs above and record the marginal-size distributions in §13; decide whether (L-circ) holds as stated (max 4 of 7 for relevant circuits) or needs the notion of "relevant circuit" made precise (a circuit is relevant iff some path segment inside $S$ passes through it, or its $z$ is an end of such a path).
2. If it holds: write the proof of (L-circ) from the rerouting mechanism (a path entering circuit $C$ at $u$ leaves from the neighbour selected by the parity of $C$'s 0-edge position), then bound the number of candidate sets that can be live at all (arc pinning, §13) and assemble (L-avg′) for the 42-vertex instance by hand, then in general.
3. If it fails: the live sets of the counterexamples (`cut_density.py`, the per-circuit marginals printed in §13) are the place to see why.
4. Independent lines left open: a 0-edge-robust counterexample seems impossible (two-point tests, §13); the enumeration `enum_covers.py` can be extended with codimension-3 pair cuts as point-killers (the 54-vertex instance uses them); `t4_realize.py`/`fullkill_sat.py` remain as verifiers.

Commands: `python3 tools/verify_fullkill.py data/<instance>.json` (verification), `python3 tools/avg_true.py data/<instance>.json 600` (averaging total), `python3 tools/cut_density.py data/<instance>.json` (killer densities), `python3 tools/lcirc_test.py data/<instance>.json` (per-circuit marginals, 20–30 min).

**Result of the 42-vertex (L-circ) test** (`tools/lcirc_n42.log`, finished after the pause was requested). 464 of the 10,104 candidate sets are live for at least one 0-edge choice (live counts from 1 to about 150 of 117,649). Over the circuits disjoint from a live set, the number of 0-edge positions occurring among its live choices is distributed as $1{:}244,\ 2{:}167,\ 3{:}115,\ 4{:}295,\ 5{:}202,\ 6{:}128,\ 7{:}18$. So **(L-circ) as stated is false**: 30% of the disjoint circuits admit 5 or 6 positions and 18 admit all 7 (those are circuits irrelevant to the set). What does hold is the average: the mean marginal is $3.43$ of $7$, i.e. a disjoint circuit halves the live set on average, and over the circuits meeting $S$ the distribution is $1{:}964,\ 2{:}349,\ 3{:}42,\ 4{:}191,\ 5{:}57,\ 6{:}12$ (the arc pinning). The right per-circuit statement is therefore an averaged one, or a statement about pairs of circuits, and the union of the live sets over all 464 candidates is what (L-avg′) must bound: $\sum_S \mathrm{live}(S)$ over these 464 sets is the exact total that `avg_true.py` estimated at $0.06\cdot4N$. First task on resumption: compute that exact total from this log's data (add a dump of the live sets to `lcirc_test.py`), then look for the structure that makes the *product* of marginals small even when single marginals are 5 or 6.

**Exact averaging total, 42-vertex instance** (`lcirc_n42.log`, rerun with live-set dump): $T=\sum_S \mathrm{live}(S)\,k(S)=30{,}314$ against $4N=470{,}596$, i.e. $T/4N=0.0644$ (the sample estimate was $0.059$). The live sets of the 464 live candidates are in `tools/oddness6_template_counterexample_n42_live.json`.

**Live-set analysis, 42-vertex instance** (`tools/live_analysis.py` on the dump):

- *Class coverage predicts failure exactly.* Of the 17,203 choices with at least one live candidate, the pattern-matching live candidates cover 1 class for 16,159, 2 classes for 1,007, 3 for 36 and 4 for exactly one choice, which is the failing choice $(4,1,6,4,2,2)$. The successive ratios (16, 28, 36) say that each further class costs an independent-looking factor of about 20 to 40: the failure is the tail of a geometric decay, not a structural accident.
- *Product structure is approximate.* The live set equals the product of its per-circuit marginals for 127 of the 464 live candidates; on average it is 0.61 of the product (minimum 0.08). So single-circuit marginals overestimate, and the true per-set density is smaller than the product of marginals; a proof via per-circuit factors would be conservative but not exact.
- *Pin avoidance is impossible.* Every one of the 42 (circuit, position) pairs is a pin of some live candidate (each circuit is pinned by 156 to 239 candidates), so no choice of 0-edges avoids all arc pins: the routing factor is essential, as the union bound already showed.
- *Live candidates per choice:* 0 for 100,446 choices (85.4%), 1 for 9,313, 2 for 4,736, 3 for 2,128, 4 for 429, and up to 9; a failure needs at least three live candidates with complementary patterns, and only one choice has four classes covered.

For (L-avg′) the operative quantities are therefore: $\mathbb E_c[\#\text{live}]\approx0.24$, $\Pr_c[\ge1\text{ live}]=0.146$, $\Pr_c[\text{2 classes}]=0.0086$, $\Pr_c[\text{3 classes}]=3\cdot10^{-4}$, $\Pr_c[\text{4 classes}]=8.5\cdot10^{-6}$. A proof that the number of classes covered at a random choice has a geometric tail with ratio below $1/4$ would already give (O6′); the mechanism behind the ratio is that a second live cut with a new pattern needs its own two pins and its own routing parities, largely independent of the first.

**The tail is generic** (`tools/tail_random.py`, four random cyclically 6-connected graphs with six 7-circuits, 3,000 0-edge choices each, all colourings of $H$ tested): with $H$ in three components (8 colourings) the killed counts are 0 for about 87% of the choices, 2 for 9 to 12%, 4 for 0.05 to 0.4%, 6 once in 12,000, and 8 never; with four components (16 colourings) up to 6 killed, with five (32) up to 12. The ratios between successive levels are about 9 and 30, the same shape as around the failing point of the counterexample. So the geometric decay of killed classes over the 0-edge space is a property of random 2-factors on cyclically 6-connected graphs, not of the special ones, and (L-avg′) is the right statement to prove: at a random 0-edge choice, the number of killed classes is at most $c\cdot r^k$ with $r$ around $1/10$; anything below $1/4$ suffices.

**The four classes behave as independent events** (`tools/class_indep.py`, 42-vertex instance, all $7^6$ choices). The average probability that a given class is killed at a random 0-edge choice is $p=0.0389$. If the four class events were independent with this $p$: $\Pr[\ge1]=0.1466$, $\Pr[{=}2]=0.0084$, $\Pr[{=}3]=2.3\cdot10^{-4}$, $\Pr[{=}4]=2.3\cdot10^{-6}$. Observed: $0.1462$, $0.0086$, $3.1\cdot10^{-4}$, $8.5\cdot10^{-6}$ (one event). The fit is close, with a mild positive correlation in the tail (a factor of about 1.3 per extra class). So (L-avg′) splits into two statements that the data supports strongly:

> **(L-p)** At a uniformly random choice of 0-edges, a fixed colouring class is killed with probability at most $p_0<1/4$ (observed: about $1/25$).
> **(L-ind)** The kill events of the four classes are at most weakly positively correlated: $\Pr[\text{all four}]\le C\,p_0^4$ with $C p_0^4<1$ (observed $C\approx 4$).

Either statement alone is not enough, but (L-p) with $p_0<1/4$ together with a correlation bound $C<p_0^{-4}$ gives a choice with a surviving class. Note that (L-p) is exactly the density question of §13 seen from the class side: the class is killed iff one of its candidate killers (with pins compatible with the choice) has the right routing.

**Per-set densities depend on the arc structure.** The conjecture "live$(S)\le N/(49\cdot16)$" (two pinned 3-arcs and four parity constraints) fails: 48 of the 464 live candidates exceed 150, and the largest live counts, 1,064 (about $N/110$) for two 21-vertex sets, 640 and 544 for 19-vertex sets, belong to sets whose arcs are long or of even length; an even arc not containing a $z$ has both ends coloured 2 for about half of the positions of its circuit's 0-edge instead of one in seven, so its pin is weak. By size, the maximal live counts are 304 (13 vertices), 280 (15), 320 (17), 640 (19), 1,064 (21). So the averaging total $T=30{,}314$ is a sum over 464 sets with densities ranging from $10^{-5}$ to $10^{-2}$, and a proof of $T<N$ needs a per-shape density bound (arcs with a $z$: $1/7$ each; even arcs without: about $1/2$; plus the routing parities) together with a bound on the number of candidate sets of each shape that are 11-cuts of $G$. The second ingredient is a statement about cyclic 11-cuts of a specific form in a cyclically 6-edge-connected cubic graph and has not been examined.

**Only six shapes carry the total.** Breaking $T=30{,}314$ down by the shape of the live candidate (number of whole circuits, arc lengths): $(1;\,3,5)$: 111 sets, 23.5%; $(2;\,2,5)$: 70 sets, 21.6%; $(2;\,2,3)$: 91 sets, 20.5%; $(1;\,3,3)$: 91 sets, 17.2%; $(2;\,3,4)$: 62 sets, 8.7%; $(1;\,5,5)$: 39 sets, 8.4%. So every live 11-cut is of one of two kinds: **one whole circuit plus two $z$-arcs** (odd lengths 3 or 5, each centred on a $z$ up to the parity freedom of a 5-arc), or **two whole circuits plus one $z$-arc plus one $z$-free even arc** (length 2 or 4, both ends colour 2 for about half of the positions of its circuit's 0-edge). The three $z$'s inside are supplied by the whole circuits and the $z$-arcs, and the even arc supplies none; the weak pin of the even arc is why the second kind has the largest live counts (up to 1,064). This is the finite list of shapes for which per-shape density bounds and counts are needed.

**Per-set live density is nearly shape-independent.** For the six shapes, pin density $A(S)/N$ (arc-end colours right and three $z$'s inside) and true live density per set:

| shape | sets | $A/N$ | live$/N$ | routing $=$ live$/A$ |
|---|---|---|---|---|
| (1; 3,3) | 91 | 0.0204 | 0.00049 | 0.024 |
| (1; 3,5) | 111 | 0.0408 | 0.00055 | 0.013 |
| (1; 5,5) | 39 | 0.0816 | 0.00056 | 0.007 |
| (2; 2,3) | 91 | 0.0408 | 0.00058 | 0.014 |
| (2; 2,5) | 70 | 0.0816 | 0.00080 | 0.010 |
| (2; 3,4) | 62 | 0.0204 | 0.00036 | 0.018 |

The pin density varies by a factor of four across shapes and the routing factor compensates almost exactly, so that **every live candidate is live for about $1/2000$ of the 0-edge choices** ($3.6$ to $8\cdot10^{-4}$). This suggests that the right per-set statement is not "pins times parities" but a direct one: a fixed 11-cut set $S$ with $|S\cap Z(c)|=3$ is bad for some colouring of $H(c)$ for at most about $N/1000$ choices $c$, whatever its shape. With that, $T\le(\#\text{live candidates})\cdot N/1000$, and (L-p) needs fewer than about 1,000 live candidate sets; the 42-vertex instance has 464. The number of live candidates is a property of $G$ (its cyclic 11-cuts of the two kinds above with a $z$-count of three achievable), and bounding it in general is the remaining combinatorial task; for large graphs the even circuits of $H$ add colouring freedom that this count does not use, so the argument as stated is for the case where $H$ is three paths, the case of all five counterexamples.

**How many candidate sets are there?** Counting the shape candidates (whole circuits plus at most two arcs, exact matching count) on random cyclically 6-connected instances: 11-cut candidates 11,139 ($n=42$, six 7-circuits), 12,135 ($n=50$, plus an 8-circuit), 16,457 ($n=54$, plus two 6-circuits), 15,767 ($n=58$, plus two 8-circuits); 7-cut candidates 49 to 72; pair-type candidates exactly $n$ in every case (one per vertex: a 4-vertex arc whose four matching edges all leave). So the raw candidate count grows slowly with $n$ and is dominated by 11-cuts; what matters for (L-p) is how many of them can be live (three $z$'s inside with the right arc colours, then the routing), which the running sweeps on the random instances will give directly (464 of 10,018 for the 42-vertex counterexample).

**Random 42-vertex instance, full sweep** (`lcirc_random42.log`, `random_six7_a.json`): 632 live candidate sets (more than the counterexample's 464), exact total $T=47{,}922$, $T/4N=0.102$. So a random 2-factor is not "safer" than the counterexample's in the averaging measure; the counterexample is special only in that its live cuts line up at one point. The averaging inequality still holds by a factor of ten.
Its live candidates have exactly the same six shapes as the counterexample's, in similar proportions (151, 114, 116, 111, 38, 102 sets), and the mean live count per candidate is $N/1552$ against $N/1801$; per-shape live densities are again within a factor of two of each other ($3.3$ to $11\cdot10^{-4}$), with the routing factor compensating the pin density as before. So the two structural facts behind the averaging argument, the six shapes and the shape-independent per-set density of order $10^{-3}$, are properties of the family, not of the counterexample.
Its class statistics: $p=0.051$ per class, $\Pr[{\ge}1]=0.19$, two classes 0.013, three $4\cdot10^{-4}$, and exactly one choice, $(5,6,1,3,5,0)$, at which pattern-matching live cuts cover all four classes. That choice is **not** a failure: there $H$ has five components (three paths and two even circuits), 32 colourings, of which 24 are balanced. The class bookkeeping is exact only when $H$ is three paths; when even circuits are present, the even-circuit colourings are an extra freedom that the four-class count ignores, and here it rescues the choice. So the random 2-factor has no failing 0-edge choice (consistent with `tail_random.py`), while having the same live-cut statistics as the counterexample's 2-factor up to a factor of 1.5. The difference between "all four classes covered" and "all colourings killed" is precisely the even circuits of $H$, which is also why all five counterexamples have $H$ equal to three paths.

**Covered classes by number of components of $H$.** Over the choices with a live candidate, (components, classes covered by live 11/7-cuts): random instance $(3,1)\,11{,}525,\ (3,2)\,540,\ (3,3)\,9,\ (3,4)\,0$; counterexample $(3,1)\,9{,}327,\ (3,2)\,380,\ (3,3)\,9,\ (3,4)\,1$; with four or more components the coverage numbers are similar but the even circuits rescue. In the dangerous three-component case the two 2-factors are statistically indistinguishable: about 9 choices with three classes covered each, and a fourth class costs another factor of roughly 10 to 40. The expected number of failing choices per random 2-factor of this profile is therefore of order $9/(10\text{ to }40)\approx0.2$ to $0.9$: **roughly one random 2-factor in two to five should have a failing 0-edge choice.** The counterexamples are not exotic; they are the typical tail. Eight random 42-vertex 2-factors are being swept exhaustively to test this prediction.

**Random 50-vertex instance (six 7-circuits and an 8-circuit), full sweep** (`lcirc_random50.log`, `random_six7p8_a.json`): only 77 live candidate sets and $T=10{,}901$, $T/4N=0.023$, a quarter of the 42-vertex values. The even circuit does not add live shapes (a whole even circuit inside $S$ brings no $z$ and, unless its matching edges pair up internally, far too many boundary edges), while it takes vertices away from the odd circuits' neighbourhoods where the 11-cuts live. Between the 42-vertex instances (464 and 632 live candidates) and this one (77), the number of live candidates is the quantity that varies most, and it decreases with the presence of even circuits; the per-set density stays of order $10^{-3}$.
Its class statistics: $p=0.017$ per class, $\Pr[{\ge}1]=0.067$, two classes 0.0022, three classes never; the independence model fits again. New shapes involving the 8-circuit appear (a 6-arc of the even circuit, 2-arcs of it), but with very few sets each; the workhorse shapes are the same as at 42 vertices.

**50-vertex counterexample, full sweep** (`lcirc_n50.log`): 299 live candidate sets, exact total $T=36{,}394$, $T/4N=0.077$ (sample estimate 0.062). Between the counterexample and the random instance of the same profile, the number of live candidates differs by a factor of four (299 against 77) while the per-set density is comparable, so on this profile the counterexample's 2-factor is genuinely richer in near-tight 11-cuts than a random one, unlike at 42 vertices where the random 2-factor had more.
Its class statistics: $p=0.048$ per class, $\Pr[{\ge}1]=0.18$, two classes 0.012, three $3.6\cdot10^{-4}$, four $1.7\cdot10^{-5}$: **two choices**, $(5,0,2,5,2,1)$ and $(5,0,2,5,2,4)$, and both are genuine failures (0 of 8 colourings balanced, checked directly). So this 2-factor has at least two failing 0-edge choices, differing only in the position of one 0-edge (by three steps on the last circuit). The "isolated point" statement of §13 holds for the 60- and 42-vertex 2-factors, which were swept exhaustively, and **not in general**: failures can cluster, at least in pairs along one coordinate. The exhaustive failure sweep of this 2-factor is running to fix the count. New shapes appear on this instance (three whole circuits with two 2-arcs; a 6-arc of the 8-circuit), each with few sets. Across the four fully swept 2-factors the per-class probability is $0.017$ to $0.051$ and the four class events are close to independent every time, with a mild positive correlation at the tail.

**Around the 50-vertex failure the landscape is not flat.** Moving one 0-edge at a time from the failing choice $(5,0,2,5,2,1)$ (killed colourings out of 8, with the number of components of $H$): circuit 5 gives $6,\mathbf 8,4,4,\mathbf 8,4,4$ over its seven positions (two failures, the rest half-killed); circuit 4 gives $6,2,\mathbf 8,4,0,4,4$ (its position 2 is a third failing neighbour along that axis, i.e. $(5,0,2,5,2,1)$ with the fourth 0-edge moved to 2 also fails); circuits 0, 2, 3 have several positions with 4 killed. Compare the 42-vertex failure, where every neighbour has at most 2 killed. So the density of near-failures around a failing choice varies strongly between 2-factors, and this one has a cluster of at least three failing choices within one step of each other. Its exhaustive failure sweep is running.

**Counts, confirmed.** The 50-vertex counterexample's 2-factor fails for **exactly two** of its 117,649 0-edge choices, $(5,0,2,5,2,1)$ and $(5,0,2,5,2,4)$ (`zero_sweep_n50.log`); the 60- and 42-vertex ones fail for exactly one. Six of the eight random 42-vertex 2-factors have been swept exhaustively: **none has a failing choice**, so the prediction "one 2-factor in two to five" was too high; the geometric-tail estimate overstated the four-class rate because the last factor is larger than the earlier ones (the fourth class needs its own two pins to be compatible with three others already pinned).

**Failures can be made to cluster.** The 0-edge-robust climber restarted from the 50-vertex counterexample with the objective "number of failing choices among the 37 one-step neighbours of the failing choice" (`robust.py`, `NEIGH=1`) raised that number from 2 to 4 within 700 matching swaps, keeping cyclic 6-edge-connectivity. So the failing set of a 2-factor is not rigidly a point or a pair: it can be grown by modifying the graph. Whether it can be grown to a positive fraction of the 0-edge space, which is what a counterexample to (O6′) for a fixed 2-factor would need, is the question the continued climb addresses; each gained failing choice so far has cost hundreds of accepted swaps.

**Stage 1 of the robust climb, verified.** The best graph of the first robust run (`tools/robust_stage1.json`, 50 vertices, cyclically 6-edge-connected, girth 6) fails for **four** 0-edge choices in the one-step neighbourhood: $(5,0,2,5,2,j)$ for $j\in\{1,3,4,5\}$, i.e. four of the seven positions of one circuit's 0-edge with the other five fixed. The climb continues from it (`robust_stage2_*.log`). All eight random 42-vertex 2-factors have now been swept exhaustively: **none** has a failing 0-edge choice.
Exhaustive sweep of the stage-1 graph (`zero_sweep_stage1.log`): exactly four failing choices in the whole 0-edge space, the four neighbours above; nothing elsewhere. The stage-2 and stage-3 climbs have since reached six failing choices within their samples.

**Stage 2 of the robust climb: seven failing choices.** The best stage-2 graph (`data/oddness6_robust_stage2b_n50.json`, 50 vertices, cyclically 6-edge-connected) fails, by exhaustive sweep, for exactly **seven** of its 117,649 0-edge choices: $(5,0,2,5,2,j)$ for $j\in\{1,2,3,4,5\}$ (five of the seven positions of one circuit's 0-edge) and $(1,0,2,5,2,1)$, $(1,0,2,5,2,3)$ (a second axis opening up). Sequence so far under matching swaps from the original counterexample: $2\to4\to7$ failing choices. Stage 3 (union of the one-step neighbourhoods of all seven, about 150 choices per evaluation) is running.

**Aiming at a whole axis.** On the seven-failure graph, five of the seven positions of circuit 5's 0-edge fail with the other five 0-edges fixed. A 2-factor together with five fixed 0-edges such that **every** position of the sixth 0-edge fails would be the cleanest robust statement: it would say that for that configuration the sixth 0-edge is irrelevant, i.e. the failure is a property of the other five circuits and the graph. The climber now also runs with the sample restricted to the seven positions of one axis (`robust.py`, `NEIGH=axis:5`), which is cheap per step; two runs from the seven-failure graph are in progress alongside the union-of-neighbourhoods climbs.

**Why the axis stops at five of seven.** On the seven-failure graph with the other five 0-edges fixed, moving circuit 5's 0-edge through its positions $j=0,\dots,6$ gives: $j=1,\dots,5$: $H$ is three paths pairing $z_5$ with vertex 16, and 7 with 30, and 26 with 5, and all colourings are killed; $j=0$: the pairing changes (7 with 16, $z_5$ with 30) and 4 of 8 colourings survive; $j=6$: an even circuit of $H$ appears (four components) and 8 of 16 survive. So the failing positions are exactly those for which the **pairing of the six ends by $H$ is unchanged**; the killers (whole circuit plus arcs around the required ends, the arc around $z_5$ sliding with it) survive the move as long as the paths keep their ends. The failing set of a 2-factor is therefore bounded by the region of 0-edge space on which the pairing is constant, and moving a 0-edge across a position where the routing switches (a path re-pairs, or an even circuit of $H$ splits off) repairs the method at once. This is the structural reason the robust climb stalls: stage 4 (union of neighbourhoods, two seeds, two hours) and the axis runs did not get beyond seven and five respectively. A 2-factor for which every 0-edge choice fails would need the pairing to be constant over all $7^6$ choices, which the rerouting mechanism (each move of a 0-edge flips the exit side of every path through that circuit) makes impossible in general. So (O6′) is safe from this line of attack, and its proof can use exactly this: **some 0-edge move changes the pairing, and a pairing change repairs the failure.**
Both axis climbs (3,000 steps each) ended at five of seven positions, and both stage-4 climbs (400 steps on 150-choice samples, two hours each) at seven failing choices in total: no further growth. All robust runs are finished.

**Every re-pairing move repairs (empirical lemma).** From every known failing 0-edge choice of the six archived instances (13 failing choices, 468 one-step moves of a single 0-edge), classifying each move by what it does to $H$ (circuit-level pairing of the six ends, number of components) and whether the method succeeds at the new choice:

| move type | repaired | still failing |
|---|---|---|
| pairing unchanged, same number of components | 96 | 15 |
| pairing changed, same number of components | **291** | **0** |
| number of components changed | 52 | 14 |

So a move that re-pairs the paths repaired the method in all 291 cases, and every failing choice has such a move (at least 16 of its 36 moves). This is the statement to prove:

> **(L-repair)** If the canonical colourings $c$ and $c'$ differ in one 0-edge and the paths of $H(c')$ pair the six ends differently from those of $H(c)$, then not both $c$ and $c'$ are failures of the fixed-2-factor template.

Together with "every choice has a re-pairing move" (a routing statement about a 0-edge shift through a circuit that some path crosses), (L-repair) gives (O6′) immediately: from a failing choice, one re-pairing step reaches a success. The 14 failures with a changed number of components are all on the seven-failure graph's axis, where positions 1 and 4 give a four-component $H$ (16 colourings) that is nevertheless fully killed; so an even circuit of $H$ does not by itself rescue, only a re-pairing did so in every case observed.

**Towards a proof of (L-repair).** Let $c$ fail with killers $S_1,\dots,S_4$ (up to complement) whose $z$-triples $T_i=S_i\cap Z$ are transversal to the old pairing $P=\{\{a,a'\},\{b,b'\},\{c,c'\}\}$ (one end of each path) and realize the four classes. Let $c'$ re-pair the ends to $P'\ne P$, say $P'=\{\{a,b\},\{a',b'\},\{c,c'\}\}$. A set $S$ can kill a colouring at $c'$ only if $S\cap Z(c')$ is transversal to $P'$ (two ends of one path are never both black). Triples transversal to both $P$ and $P'$ are exactly $\{a,b',x\}$ and $\{a',b,x\}$ with $x\in\{c,c'\}$, which up to complement are **two** of the four classes of $P'$. So the old killers can account for at most two of the four new classes, and the other two can be killed at $c'$ only by sets that were not killers at $c$: sets whose triple was non-transversal for $P$ (they could not be tight at $c$ because their pattern was impossible), or sets that involve the moved circuit (an arc around the new $z$, or arc ends whose colours changed). Everything away from the moved circuit is unchanged by the move: the matching, the colours, the other $z$'s, hence the tightness conditions of every set not meeting the moved circuit. So (L-repair) reduces to a local statement: **a single 0-edge move on circuit $C$ cannot create, out of sets that were not tight at $c$, tight cuts for both of the two new classes that the old killers cannot reach**, unless it undoes the tightness of the old ones. The data says this never happens (291 of 291), and the structure of tight sets (§13: whole circuits plus two arcs, arcs centred on $z$'s) makes it a finite case analysis around $C$: the only sets whose status changes are those with an arc on $C$ or with $z_C$ inside a whole circuit... this is the next thing to write down carefully.

**Second 60-vertex counterexample, exhaustive sweep** (`zero_sweep_n60b.log`): **four** failing choices in two clusters, $(4,5,2,4,2,0)$, $(4,5,2,4,2,4)$ and $(4,6,2,4,1,3)$, $(4,6,2,4,1,6)$; the clusters differ in three coordinates, so failing sets need not be connected. Repair test from these four: `{('components changed', 'repaired'): 34, ('re-paired', 'repaired'): 70, ('same pairing', 'repaired'): 36, ('same pairing', 'fails'): 4}`; every re-pairing move repaired here as well.

**Consolidated (L-repair) tally** over the sixteen known failing choices of the six archived instances (576 one-step moves; `tools/repair_test.py` on all archives): pairing unchanged: 123 repaired, 18 still failing; **pairing changed: 345 repaired, 0 failing**; number of components changed: 76 repaired, 14 failing. Every failing choice has between 16 and 24 re-pairing moves among its 36.

**What happens at a re-paired neighbour.** For the 345 re-pairing moves from the sixteen failing choices: at the new choice, killed colourings are 0 in 97 cases, 2 in 88, 4 in 123, and up to 6 of 8 (or 10 of 16 when $H$ has four components) in the rest, never all. In 294 of the 345 cases every killer at the new choice is one of the **old** killer sets (as vertex sets) that happens to remain tight for the new pairing; in 51 cases at least one killer is a set that was not a killer before (a set "born" by the move, typically with an arc around the moved $z$). So the sketch is right in its main line (old killers reach at most two of the four new classes, and the count of killed colourings is indeed at most 6 of 8 whenever only old killers act) but a proof must also control the newborn killers: they occur in 15% of the re-pairings and never complete a cover in the data.

**Check of the two-class bound.** At re-paired neighbours where only old killer sets act, the killed colourings are at most half of all colourings in every case but two: on the 54-vertex instance, two re-pairing moves leave 6 of 8 colourings killed by old sets alone. That instance is the one whose killers include pair-type 6-cuts crossed by the third path (codimension 3), and the transversal-triple argument was made for 11-cuts, whose kill pattern is a triple of ends; a pair cut kills a class determined by its two ends together with the parity of the third path's crossing, which a re-pairing can preserve differently. So (L-repair) has two cases: covers by 11-cuts (and 7-cuts), where old killers reach at most two new classes, and covers involving pair cuts, where a separate argument is needed. In neither case did a re-pairing move leave all colourings killed.

**Lemma 13.1 (old killers after a re-pairing).** Let $c$ and $c'$ be 0-edge choices such that $H(c)$ and $H(c')$ both consist of three paths, with pairings $P\ne P'$ of the six odd circuits. Let $S$ be a set that is a tight 11- or 7-cut at $c$ (so $S\cap Z(c)$ is a triple transversal to $P$) and suppose $S\cap Z(c')$ consists of the $z$'s of the same three circuits. Then $S$ can kill at $c'$ only a class among **two** fixed classes of $P'$, the same two for every such $S$. *Proof.* Write $P=\{\{a,a'\},\{b,b'\},\{c_1,c_1'\}\}$. Two distinct perfect matchings of six elements share at most one pair; after renaming, $P'=\{\{a,b\},\{a',b'\},\{c_1,c_1'\}\}$ (if they share no pair the same count applies to the crossed pairs). A triple that kills at $c'$ must be transversal to $P'$, since two ends of one path have opposite colours. Triples transversal to both $P$ and $P'$ are $\{a,b',x\}$ and $\{a',b,x\}$ with $x\in\{c_1,c_1'\}$, which up to complement are two classes of $P'$. $\square$

So after a re-pairing at least two of the four new classes can be killed only by sets that were not killers at $c$ (newborn killers, necessarily involving the moved circuit, since nothing else changed) or by pair-type cuts, whose kill pattern is not a transversal triple. (L-repair) is thereby reduced to: newborn 11/7-cut killers cannot cover two classes by themselves after a single move, and pair-type killers behave (the 54-vertex instance shows old pair cuts keeping three classes killed after a re-pairing, never four).

**Re-pairing moves always exist.** Over 150 random 0-edge choices on each of three instances (the 42-vertex counterexample, the random 42- and 50-vertex instances), the number of re-pairing moves among the 36 one-step moves ranges from 2 to 32 with a median around 21; no choice had none. So the second ingredient of (O6′) via (L-repair) holds empirically with a large margin, and it should be provable from the rerouting mechanism: moving the 0-edge of a circuit that some path crosses in the interior flips that path's exit from the circuit.

## 15. Resume checklist (updated 2026-09-13, afternoon)

**State.** Template refuted (§12; six archived instances in `data/`, each with a `fails` list of its failing 0-edge choices, exhaustively swept except the 54-vertex one). Computer-verified exclusions §10. The averaging plan §13 is now organised around (L-repair): every one-step 0-edge move that re-pairs the paths repaired the method in all 345 observed cases; Lemma 13.1 explains half of it (old 11/7-cut killers reach at most two new classes); what remains is newborn killers around the moved circuit and the pair-cut case. Re-pairing moves exist from every choice (empirically 2 to 32 of 36).

**Running when this was written:** completion runs generating further full kills (`complete_more_C.log`, `_D.log`, `_E.log`; outputs `fullkill_complete_<seed>.json`), with a chained `repair_test.py` over the new full kills whose result lands in the task output; if the chained job is lost, run `python3 tools/repair_test.py tools/fullkill_complete_1*.json` by hand.

**To pick up.**
1. Extend the (L-repair) tally with every new full kill; any re-pairing move that leaves a failure would refute (L-repair) as stated and should be dissected with `survivors.py`/`anatomy.py`.
2. Prove the remaining two ingredients of (L-repair): (a) a single 0-edge move on circuit $C$ creates tight 11/7-cuts only among sets whose arcs or $z$-membership involve $C$, and such newborn sets cannot cover two classes of the new pairing by themselves; (b) the pair-cut case. Then (c) existence of a re-pairing move from any choice (rerouting of a path crossing the moved circuit). Together these give (O6′) and hence (O6) for graphs with a 2-factor of at most six odd circuits and $H$ three paths; the case with even circuits of $H$ needs the extra freedom handled.
3. Tools: `repair_test.py` (classification), `lcirc_test.py` (full live-set sweep, 20–45 min), `shape_table.py`, `class_indep.py`, `live_analysis.py`, `cut_density.py`, `avg_true.py`, `robust.py` (NEIGH=1/2/axis:k), `complete.py`, `verify_fullkill.py`, `zero_sweep.py`.

*Caution on ingredient (a).* A set far from the moved circuit $C$ can change status too: the tightness of $S$ depends on the colours of the path vertices inside $S$, and moving $C$'s 0-edge reroutes every path that crosses $C$, which changes the parity of positions along that path beyond $C$ and hence the black/white pattern of its segments inside any $S$ it visits. So newborn killers need not touch $C$; they are the sets visited by a path through $C$ whose segment parities flip. The correct local statement is about paths, not distance: only sets whose inside segments lie on paths through $C$ can change status, and their new status is determined by the parity change of those paths. This is still a finite bookkeeping around the three paths, but the proof of (L-repair) must be organised along paths rather than around $C$.

**Newborn killers lie on paths through the moved circuit, always.** For the 82 newborn killer sets observed at re-paired neighbours (51 moves), every one contains a segment of a path that crosses the moved circuit at the new choice; 68 of them also meet the moved circuit itself, 14 do not. No newborn killer lies only on paths that avoid the moved circuit. This confirms that the correct locality for (L-repair) is along the paths: a single 0-edge move changes the status only of sets whose inside segments lie on the paths through the moved circuit, through the flip of position parity on those paths.

**When does moving a 0-edge re-pair the paths?** Over 120 random choices on the 42-vertex counterexample, tabulating per circuit $C$ the number of paths whose interior crosses $C$ against the number of re-pairing moves among the six moves of $C$'s 0-edge: no interior crossing gives no re-pairing move (9 of 9); one crossing path gives 0 re-pairing moves in 82 cases and 1 to 6 in 149; two crossing paths give 1 to 6 (never 0); three give 3 or 5. The mechanism: flipping the colour class of $C$ re-glues the $H$-pieces on $C$; with two or more paths through $C$ their tails can be exchanged, which re-pairs; with one path the re-gluing may or may not change its far end. Since three paths on six 7-circuits with no even circuit of $H$ cover all 42 vertices, each circuit carries three interior visits (two vertices each) plus its $z$; a choice with **no** re-pairing move at all would need every circuit to be visited by a single path, i.e. each path confined to two circuits, a segregated configuration that I have not seen and that looks incompatible with four tight 11-cuts (each needs arcs on two circuits with the required ends). The existence of a re-pairing move from every *failing* choice is therefore plausible but is a separate statement to prove, most likely by showing that a segregated configuration cannot be a failure.
Segregated configurations do occur: among 1,500 random choices, 29 (counterexample graph) and 20 (random graph) have $H$ equal to three paths with every circuit's interior visited by at most one path. None of these 49 kills more than 2 of the 8 colourings. So the empirical picture for the existence ingredient is: a failing choice always has a re-pairing move, because failures need at least two paths through some circuit (the arcs of the killers pin the ends of two different paths on circuits whose interiors carry those paths), and configurations without such a circuit do not come close to failing.

**Two further full kills** (60 vertices, from the `straddleC` near miss, seeds 102 and 103 of `complete.py`, both verified, $H$ three paths): from each failing choice, 21 re-pairing moves, all repairing; 10 same-pairing and 5 component-changing moves, all repairing. Running (L-repair) tally: **387 re-pairing moves, 387 repairs.**
A third new full kill (54 vertices, from the `straddleA` near miss, seed 106, verified, $H$ three paths): 30 re-pairing moves, all repairing. **Final tally for today: 19 failing choices, 417 re-pairing moves, 417 repairs; no re-pairing move has ever left a failure.** The other four completion runs of the batch stopped at 6 of 8.

**Lemma 13.2 (a 0-edge move is a cyclic shift of the visits).** Let $C=v_0v_1\cdots v_{L-1}$ be an odd circuit with 0-edge $e_j=v_jv_{j+1}$, so $z_C=v_j$, and the colour-2 edges of $C$ are $e_{j+1},e_{j+3},\dots,e_{j+L-2}$; the paths of $H$ visit $C$ along these edges, the $k$-th visit being $(v_{j+2k-1},v_{j+2k})$, $k=1,\dots,(L-1)/2$, entered and left by the matching edges at its two vertices. Moving the 0-edge to $e_{j+1}$ makes $z_C=v_{j+1}$ and the colour-2 edges $e_{j+2},e_{j+4},\dots,e_{j+L-1}$: the visits become $(v_{j+2k},v_{j+2k+1})$, i.e. each new visit joins the exit vertex of the old $k$-th visit to the entry vertex of the old $(k{+}1)$-st, and the last joins the old last exit to the old $z_C$; the old first entry $v_{j+1}$ becomes the new $z_C$. Everything outside $C$ is unchanged. (Verified on all 126 (circuit, position) cases of the 42-vertex instance.) *Proof.* Immediate from the definition of the canonical colouring. $\square$

Consequently the effect of the move on $H$ is determined by the **external connection pattern** at $C$: the $L$ matching edges at $C$ lead into pieces of $H$ that either return to $C$ at another attachment vertex or end at one of the other five $z$'s; the move re-glues these pieces by the shift above. Whether the pairing of the six ends changes is a finite question about this pattern, which is the right formulation of the existence statement for (L-repair).

**Lemma 13.3 (which moves can preserve the pairing), by enumeration.** Model the external connection pattern at a 7-circuit $C$ as a perfect matching on its seven attachment vertices $a_0=z_C,a_1,\dots,a_6$ together with the five other ends $E_1,\dots,E_5$ (a piece joins two attachment vertices, an attachment vertex to an end, or two ends for a path avoiding $C$), with old visits $(a_1a_2),(a_3a_4),(a_5a_6)$ and, after the move, $(a_2a_3),(a_4a_5),(a_6a_0)$ with new $z_C=a_1$ (Lemma 13.2). Of the 7,200 patterns for which the old $H$ is three paths: 3,840 re-pair the ends, 1,080 keep the pairing, 2,280 create an even circuit of $H$. Split by the number of distinct paths that meet $C$ away from $z_C$: **three paths: 600 re-pair, 120 create a cycle, none keeps the pairing**; two paths: 2,520 / 1,260 / 540; one path: 720 / 900 / 540. So a move on a circuit crossed by all three paths always changes $H$'s pairing or its number of components, and a pairing-preserving move requires a circuit met by at most two paths. Combined with (L-repair) this gives the shape of the existence statement: a failing choice needs a circuit visited by at least two paths (the arcs of its killers), and among the moves on such circuits at most one in eight patterns preserves the pairing.
